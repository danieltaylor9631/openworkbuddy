# OpenWorkBuddy 后续优化方案（面向 2026–2027 Agent 趋势）

**文档编号**：3SOLUTION.md  
**立场**：本项目主干完成于 2025–2026 上半年的「本地办公 Agent」浪潮中。当时行业焦点是：工具循环、MCP、桌面落地、技能 Markdown。进入 2026 下半年与 2027，行业焦点转移到**上下文工程、可验收目标、多 Agent 有界编排、计算机使用治理、评测门禁、成本路由、企业身份与沙箱、跨设备安全中继**。本文不推翻「本地文件交付、零构建、JSON 可猫」的产品性格，只说明如何在这性格上长出下一代能力。

**方法**：每一节包含趋势为何成立、本仓库已有锚点、缺口、建议方案、验收指标、风险、分步。验收默认包含 `npm test` 不回归，核心提示词/工具改动跑 `npm run eval`。

**明确不建议**：微服务拆分、强制云、引入重前端框架、自动改生产代码、静默降级用户选的模型、自动合并 git 分身。这些与已写明的架构红线冲突。

---

## 总览路线

建议三条并行轨道：

1. **质量轨道**（对标 Claude Code / Codex / pi）：ContextPack、Goal/SPEC、评测门禁、假绿物理检查。  
2. **创作轨道**（短剧与内容）：画布 DAG 重算、引用完整性、费用预估、角色一致性抽检。  
3. **组织轨道**（团队与远程）：Review Inbox、OIDC 可选、中继 MVP、沙箱严格档。

个人版默认路径保持「装上就能用」。企业能力全部以可选模块出现，以免拖死桌面双击体验。

## 1. 上下文工程从「裁最老」到「可证明的工作记忆」

### 1.1 趋势背景

2025–2026 业界把 context engineering 当成主战场：Anthropic、OpenAI 与开源 Agent（pi、Claude Code、Codex）都把压缩、备忘录、可重取工具结果当成产品质量，而不是提示词技巧。长任务小时级已是办公 Agent 常态。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 1.2 现状锚点（去年已做）

本项目已有 compactHistory、trimHistory、PROGRESS.md 注入、工具结果分级压缩、文件清单机械提取。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 1.3 缺口

缺少跨会话的「项目工作记忆」版本化；压缩质量无离线黄金集；裁剪仍可能丢掉第 3 步约束（虽有 long-haul 题但基线分未刷满）；多模态历史（图、分镜）尚未纳入同一套预算模型。

### 1.4 方案

建立 ContextPack：把目标、约束、已交付路径、未决决策做成结构化对象，压缩只对该对象做，正文当附件可重取。为每类工具定义 TTL 与重取代价。把 long-haul eval 纳入每次改 agent.js 必跑。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 1.5 如何算做完

long-haul 题约束保持率；平均 token/成功任务；用户插话纠偏次数。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 1.6 风险与不做什么

摘要模型幻觉会污染工作记忆，必须机械字段与模型字段分离。

### 1.7 建议步骤

1) 定义 ContextPack schema 与序列化进 system 的预算 2) 工具结果分类 TTL 3) 压缩黄金集 20 条 4) 与画布节点指纹打通。

### 1.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 1.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 1.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 2. 规范驱动与 Goal 模式产品化

### 2.1 趋势背景

Spec-driven / goal-conditioned agents 成为企业采购话术：先验收标准后执行。OpenAI o 系列与各种 judge 模型让「机器验收」变得便宜。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 2.2 现状锚点（去年已做）

Goal 模式已拆清单、文件实测、可选 Jev、最多 3 轮补跑。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 2.3 缺口

验收标准自然语言偏软；与短剧画布节点完成态未统一；CLI/IM 入口的 Goal 壳曾漂移，需持续用 modes.js 守住；缺少规格文件（SPEC.md）作为一等公民。

### 2.4 方案

让项目可放置 SPEC.md/ACCEPTANCE.md，Goal 启动时自动加载。清单项类型化：文件存在、正则、HTML 市州数量、截图断言。画布节点完成态复用同一套。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 2.5 如何算做完

Goal 任务一次通过率；补跑轮次分布；误绿率。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 2.6 风险与不做什么

过严的机器验收导致永远补跑烧钱，需按任务类型配置严格度。

### 2.7 建议步骤

1) SPEC 加载 2) 断言库 3) 与 Jev 确定度显示统一 4) 文档教用户怎么写可验收目标。

### 2.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 2.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 2.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 3. 多 Agent 编排从接力名单到有界图

### 3.1 趋势背景

2026 年多 Agent 讨论从「多几个角色」转向有界通信、共享黑板、避免自嗨循环。生产系统强调 supervisor + specialists + 预算。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 3.2 现状锚点（去年已做）

已有 delegate_to_expert / team 顺序接力、共享工作目录、token 预算整树共享。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 3.3 缺口

没有显式通信图、没有专家级只读沙箱、接力无法并行、专家失败重试策略粗糙、缺少对「专家把记忆写成能力已修复」这类污染的隔离。

### 3.4 方案

引入 TeamGraph：节点=专家，边=工件类型。默认同目录但可选子目录隔离再 merge。并行无依赖专家。每个专家独立死循环计数。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 3.5 如何算做完

团队任务墙时与费用；工件交接缺失率。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 3.6 风险与不做什么

并行写同一文件冲突，需与 worktree/所有权模型结合。

### 3.7 建议步骤

1) 工件清单协议 2) 可选隔离目录 3) 可视化团队进度 4) eval 多专家题。

### 3.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 3.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 3.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 4. 计算机使用与浏览器 Agent 收敛

### 4.1 趋势背景

Computer-use 模型与托管浏览器（Anthropic、OpenAI、开源 Skyvern 等）在 2025 爆发。办公场景仍更需要「读动态页 + 偶尔点击」而不是全桌面接管。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 4.2 现状锚点（去年已做）

已有 fetch、render_page、chrome_cdp、Electron 隐藏窗、闲置关闭浏览器进程组。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 4.3 缺口

CDP 脚本脆弱；无录制回放；无站点级 cookie 保险柜；无「最小点击」策略；浏览器崩溃隔离不足。

### 4.4 方案

把常用站点做成连接器而不是让模型每次想选择器。CDP 操作进审批。会话级浏览器配置文件与用户日常浏览器分离。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 4.5 如何算做完

抓取成功率；浏览器残留进程数；错误点击率。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 4.6 风险与不做什么

全自动点支付/删除按钮不可接受，必须名单。

### 4.7 建议步骤

1) 站点连接器 2) 浏览器生命周期看门狗加强 3) 操作审批 4) e2e 对 fixture 页而不是活网。

### 4.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 4.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 4.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 5. 记忆三层：情景 / 语义 / 程序

### 5.1 趋势背景

认知科学口径进入产品：episodic（发生过什么）、semantic（事实与偏好）、procedural（怎么干成的技能）。只做向量搜索不够。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 5.2 现状锚点（去年已做）

手写区+条目+向量+命中回写淘汰+拒密钥与过期能力断言。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 5.3 缺口

没有情景记忆（某次任务的经过）；程序记忆与 skills 两套；跨设备记忆同步弱；嵌入失败静默。

### 5.4 方案

任务结束写一页 episodic 摘要进会话包，不进全局语义。把成功轨迹蒸馏为 skill 草案走 skill-guard 人审。嵌入连续失败要进设置页。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 5.5 如何算做完

memory-recall 题；错误记忆导致的返工次数。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 5.6 风险与不做什么

自动写技能可能固化错误流程。

### 5.7 建议步骤

1) episodic 摘要 2) 技能蒸馏人审 3) 嵌入健康 4) 导入更多第三方记忆格式。

### 5.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 5.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 5.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 6. 工具生态：MCP 与 Plugins 的治理

### 6.1 趋势背景

MCP 成为事实标准；Agent Plugins 1.0 试图解决技能+工具打包。2026 的问题是供应链安全与权限最小化。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 6.2 现状锚点（去年已做）

已有 MCP stdio/HTTP、广场、Plugins、skill-guard、toolward 可选。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 6.3 缺口

连接器权限过粗；无工具级用户可见权限清单；第三方 MCP 更新无签名；广场 50+ 连接器质量不齐。

### 6.4 方案

每个 MCP 工具安装时生成权限清单（读盘/出网/密钥）。默认沙箱工作区。插件更新校校验和。对高危工具强制 ask 档。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 6.5 如何算做完

安装拦截真阳性/假阳性；MCP 故障人话率。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 6.6 风险与不做什么

过严导致广场不可用。

### 6.7 建议步骤

1) 权限清单 UI 2) 校验和 3) 连接器分级 4) 与企业后台策略打通。

### 6.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 6.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 6.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 7. 评测驱动开发成为发版门禁

### 7.1 趋势背景

τ-bench、SWE-bench、内部黄金任务集成为 Agent 团队的 CI。pass@k 与失败码比「感觉聪明」重要。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 7.2 现状锚点（去年已做）

eval v2、15+ 题、AI 评委、基线、CONTRIBUTING 触发条件、隔离数据目录。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 7.3 缺口

新题无基线分；CI 默认只跑零 token 测试；提示词微调缺少自动回归报告贴 PR。

### 7.4 方案

对核心路径维护小而稳的付费 eval 夜间任务；PR 机器人贴与基线 diff。失败码映射到 evolve 信号。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 7.5 如何算做完

基线不回归；CI 时长。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 7.6 风险与不做什么

评测集过拟合。

### 7.7 建议步骤

1) 夜间 eval 2) PR 评论 3) 轮换题 4) 人工抽检。

### 7.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 7.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 7.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 8. 可中断、可插话、可回放的 Human-in-the-loop

### 8.1 趋势背景

2026 好 Agent 的标志是随时能打断、改方向、批准高影响动作，并且事后能看懂。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 8.2 现状锚点（去年已做）

插队、ask_user、审批 diff、检查点、SSE 直播、宠物提醒。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 8.3 缺口

审查队列未集中；IM 上审批体验不均；长时间等待无摘要心跳；移动端操作集不完整。

### 8.4 方案

做一个跨 Web/CLI/IM 的 Review Inbox：命令、外发、发布、删除。心跳每 N 分钟推「仍在做 X，已有文件 Y」。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 8.5 如何算做完

高影响动作未经批就执行的次数应为 0；插话成功率。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 8.6 风险与不做什么

问得太多会烦，需按档位。

### 8.7 建议步骤

1) Inbox API 2) IM 卡片批准 3) 心跳 4) 移动端补齐 rewind/approve。

### 8.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 8.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 8.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 9. 成本路由、缓存与模型分工

### 9.1 趋势背景

推理模型贵；例行工具调用应用小模型。缓存（提示前缀、结果缓存）是利润。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 9.2 现状锚点（去年已做）

生成结果缓存、LLM cached 入账 1/10、failover、按能力配不同模型、健康账本。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 9.3 缺口

无自动把「分类/抽取」路由到小模型；无提示前缀稳定化策略文档；画布重算尚未全图指纹。

### 9.4 方案

TaskRouter：规划用强模型，抽取用小模型，判断用 Jev。系统提示词分段稳定以吃前缀缓存。画布全图下游重算。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 9.5 如何算做完

单位成功任务费用；缓存命中率（只用记过 cached 的样本）。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 9.6 风险与不做什么

小模型工具调用格式更差，需还原器与重试。

### 9.7 建议步骤

1) 路由表 2) 提示分段 3) 画布指纹 4) 费用看板。

### 9.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 9.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 9.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 10. 安全沙箱与供给链

### 10.1 趋势背景

Agent 握有 shell 是 2026 最大企业顾虑。趋势是系统级沙箱（OS、容器、seccomp）加政策即代码。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 10.2 现状锚点（去年已做）

软护栏已强：黑名单、审批、DANGER、技能静态扫描。无 OS 沙箱。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 10.3 缺口

run_shell 仍是真 shell；服务器多租户同进程；文件系统隔离靠路径检查。

### 10.4 方案

服务器版默认 Docker 内非 root + seccomp；桌面版提供「严格」档用 OS 用户或 bubblewrap（Linux）。路径检查形式化测试生成。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 10.5 如何算做完

逃逸用例全红；渗透清单。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 10.6 风险与不做什么

沙箱过严导致本机办公（打开 WPS）失败。

### 10.7 建议步骤

1) 部署加固默认 2) 严格档 3) 生成式路径逃逸测试 4) 第三方技能签名。

### 10.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 10.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 10.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 11. 可观测性：Trace 成为账本不是装饰

### 11.1 趋势背景

OpenTelemetry for agents、Langfuse、本地 jsonl 并存。采购方要成本与责任追溯。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 11.2 现状锚点（去年已做）

本地 Trace + Langfuse + 逐步耗时 + 假绿裁定。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 11.3 缺口

与画布节点双向跳转未完成；筛选器弱；PII 红线不完善；分布式（CLI 在家、服务在公司）无中继。

### 11.4 方案

统一 Run 对象：id、车道、引擎、费用、工件、失败码。UI 从文件跳 Trace。PII 过滤规则。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 11.5 如何算做完

从任意成果反查生成参数的成功率 100%。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 11.6 风险与不做什么

Trace 含隐私外流。

### 11.7 建议步骤

1) Run schema 2) 跳转 3) PII 4) 导出合规。

### 11.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 11.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 11.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 12. 技能即程序：从 Markdown 到带测试的包

### 12.1 趋势背景

Skill 市场兴起。赢家是可测、可版本、可回滚的技能包，而不是一段神秘提示词。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 12.2 现状锚点（去年已做）

Markdown 热加载是产品性格；skill-creator；安检。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 12.3 缺口

无技能级测试夹具；无版本锁定；无 A/B。

### 12.4 方案

允许 skill.md + tests/*.json 输入输出夹具，发布前跑。版本号与兼容引擎声明。保留无构建作者体验。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 12.5 如何算做完

技能回归失败拦截率。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 12.6 风险与不做什么

提高写作门槛。

### 12.7 建议步骤

1) 可选 tests 目录 2) 广场展示版本 3) 回滚按钮。

### 12.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 12.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 12.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 13. 流式 UX 与「过程可信」

### 13.1 趋势背景

用户信任来自过程可见：谁在调什么、钱花在哪、文件是否真的在。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 13.2 现状锚点（去年已做）

过程卡、流式 SVG、来源列表、耗时、宠物。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 13.3 缺口

移动端过程密度过高；失败下一步有时仍太技术；多任务切换状态偶发不一致。

### 13.4 方案

过程卡信息分级：一句话人话 + 可展开技术细节。统一 Run 状态机。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 13.5 如何算做完

用户因「以为完成了」而提交空文件的次数。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 13.6 风险与不做什么

动画过多影响无障碍。

### 13.7 建议步骤

1) 文案分级 2) 状态机 3) 无障碍抽检。

### 13.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 13.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 13.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 14. 桌面与环境 Agent（宠物、通知、睡眠）

### 14.1 趋势背景

Ambient agent：在边缘提醒，而不是抢焦点。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 14.2 现状锚点（去年已做）

宠物、通知、awake、合盖顺延。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 14.3 缺口

宠物默认关闭正确，但发现成本高；无勿扰日程；无多显示器策略。

### 14.4 方案

与系统勿扰同步；仅在 ask_user/审批时主动。提供无宠物的轻托盘。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 14.5 如何算做完

错过审批的比例。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 14.6 风险与不做什么

过度拟人引发反感。

### 14.7 建议步骤

1) 托盘模式 2) DND 3) 审批声音一次。

### 14.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 14.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 14.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 15. 企业多租户与钥匙管理

### 15.1 趋势背景

2026 企业要 BYOKEY、审计、SSO、数据驻留。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 15.2 现状锚点（去年已做）

多租户、后台、虚拟 Key、RBAC、离职回执已落地。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 15.3 缺口

无 SSO/OIDC；无 HSM；渠道 Key 仍明文文件（0600）；无 SCIM。

### 15.4 方案

OIDC 登录；Key 用系统钥匙串或外部密钥管理可选；SCIM 停用同步离职。保持开源版可纯文件运行。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 15.5 如何算做完

入职时长；离职权限残留为零。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 15.6 风险与不做什么

企业功能拖死个人版简单性。

### 15.7 建议步骤

1) 可选 OIDC 2) 钥匙串 3) SCIM 4) 文档分个人/企业路径。

### 15.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 15.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 15.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 16. 本地优先 vs 云托管的产品分裂管理

### 16.1 趋势背景

市场同时要「数据不出机」和「随时手机」。中继是下一阶段。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 16.2 现状锚点（去年已做）

本地默认；Docker；配对在同一机器目录。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 16.3 缺口

跨机器无安全中继；家庭电脑与公司 VPS 会话不通。

### 16.4 方案

做可选中继：端到端加密，服务端不可读任务正文。明确威胁模型。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 16.5 如何算做完

远程任务成功率；中继零知识审计。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 16.6 风险与不做什么

自建中继运维成本。

### 16.7 建议步骤

1) 威胁模型文档 2) 中继 MVP 3) 与现有 pair 令牌兼容。

### 16.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 16.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 16.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 17. 推理模型与工具调用可靠性

### 17.1 趋势背景

o 系列、R1、Claude 扩展思考改变延迟结构：先想后调工具。工具 JSON 仍是事故源。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 17.2 现状锚点（去年已做）

thinking 档、工具还原、repairToolPairs、未知工具建议近名。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 17.3 缺口

推理过程过长烧超时；思考内容是否进 Trace 的隐私政策不清；JSON 模式未对所有渠道开启。

### 17.4 方案

按渠道默认 thinking；超时与 stall 分离；JSON schema 严格模式可配。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 17.5 如何算做完

工具解析失败率；stall 误杀率。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 17.6 风险与不做什么

关思考导致计划质量下降。

### 17.7 建议步骤

1) 渠道档案 2) schema 严格 3) 超时分层。

### 17.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 17.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 17.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 18. 结构化输出与办公文件语义

### 18.1 趋势背景

交付物要从「能打开」升级到「结构正确」：目录、样式、公式、无坏引用。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 18.2 现状锚点（去年已做）

写后自检 JS/JSON/HTML/PY/SH；Office 真库生成；预览。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 18.3 缺口

PPT 设计质量无自动审美；docx 修订流程弱；Excel 口径检查弱。

### 18.4 方案

交付物 linter：pptx 页数、字体、对比度；xlsx 公式与死数字；docx 样式。纳入 Goal 断言。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 18.5 如何算做完

办公文件打不开率 0；公式死数字检出。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 18.6 风险与不做什么

审美 linter 误报。

### 18.7 建议步骤

1) 交付物规则包 2) Goal 集成 3) 用户可关。

### 18.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 18.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 18.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 19. 视频与多模态流水线工业化

### 19.1 趋势背景

短剧是变现场景。工业界强调节点缓存、角色一致、成本面板。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 19.2 现状锚点（去年已做）

画布、五协议、缓存、分镜版本、拼片计划、并发生成。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 19.3 缺口

下游自动重算未完成；素材引用检查弱；角色一致性无度量；成本预估不准。

### 19.4 方案

节点指纹驱动 DAG 重算；删除素材时引用检查；用 look_at_image 做角色一致性抽检进 Goal。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 19.5 如何算做完

单集费用；误重跑次数；角色漂移投诉。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 19.6 风险与不做什么

抽检本身花钱。

### 19.7 建议步骤

1) DAG 2) 引用图 3) 抽检抽样率可配。

### 19.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 19.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 19.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 20. 并行隔离与所有权

### 20.1 趋势背景

多 Agent 并行必须先有所有权，否则文件战争。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 20.2 现状锚点（去年已做）

worktree；output ownership 测试；会话目录。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 20.3 缺口

非 git 目录并行仍可能互踩；画布多用户同时编无 OT。

### 20.4 方案

非 git 也克隆工作副本；画布短期单写者锁。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 20.5 如何算做完

并行任务互踩文件数 0。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 20.6 风险与不做什么

磁盘翻倍。

### 20.7 建议步骤

1) 副本策略 2) 锁 3) UI 提示谁在写。

### 20.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 20.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 20.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 21. 自进化从提示词到代码修补建议

### 21.1 趋势背景

Self-improving agents 热。危险是自动改生产代码。正确顺序仍是 Warp 式：先验收台，再建议，人合并。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 21.2 现状锚点（去年已做）

evolve 信号/反馈/闸门/人审/打分已通；曾挖出 zsh glob、假阳性等真修复。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 21.3 缺口

不能自动提 PR；代码类信号只进 notes；夜间复盘默认关。

### 21.4 方案

对 prompt 档继续自动化；对 code 档生成「建议 issue 草稿」而不是补丁。保持永不自动生效。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 21.5 如何算做完

规则有效率；错误下架率（已修过的时间口径）。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 21.6 风险与不做什么

开放自动改代码。

### 21.7 建议步骤

1) issue 草稿 2) 保持人审 3) 时间口径测试不回退。

### 21.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 21.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 21.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 22. 语音与会议 Agent

### 22.1 趋势背景

会议纪要、语音指令是办公高频。端侧 STT 进步。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 22.2 现状锚点（去年已做）

transcribe + meeting-minutes 技能。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 22.3 缺口

无实时语音打断；无说话人分离保证；无日历集成。

### 22.4 方案

可选本地 Whisper；纪要走 Goal 检查待办是否都有负责人。日历只读连接器。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 22.5 如何算做完

纪要待办漏检。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 22.6 风险与不做什么

录音隐私。

### 22.7 建议步骤

1) 本地 STT 选项 2) 待办断言 3) 明确录音保留政策。

### 22.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 22.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 22.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 23. 端侧小模型与离线降级

### 23.1 趋势背景

笔记本 NPU / Ollama / llama.cpp 让离线成为卖点。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 23.2 现状锚点（去年已做）

Ollama 渠道预设。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 23.3 缺口

无自动在云挂时降到本地（产品红线是不静默降级）；无工具调用微调小模型推荐列表。

### 23.4 方案

显式「离线档」一键切 Ollama + 禁用出网工具。不要偷偷切。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 23.5 如何算做完

离线档任务可完成比例（Ask/总结类）。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 23.6 风险与不做什么

用户以为还在用强模型。

### 23.7 建议步骤

1) 离线档 UI 大声提示 2) 工具裁剪 3) 文档。

### 23.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 23.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 23.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 24. Agentic RAG 与资料库

### 24.1 趋势背景

RAG 从一次检索变成多次带工具的阅读。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 24.2 现状锚点（去年已做）

资料库 list/read/search、会话搜索。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 24.3 缺口

无嵌入索引大库；无引用强制；大 PDF 策略弱。

### 24.4 方案

资料库可选嵌入；回复来源必须是工具真实访问；PDF 先落地再分段读。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 24.5 如何算做完

虚假来源率（已有来源列表机制，需扩展到资料库）。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 24.6 风险与不做什么

嵌入隐私。

### 24.7 建议步骤

1) 可选索引 2) 来源强制 3) PDF 管道。

### 24.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 24.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 24.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 25. 画布即工作流图

### 25.1 趋势背景

节点图被广泛用于视频与数据 ETL。Agent 应把图当可执行 IR。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 25.2 现状锚点（去年已做）

JointJS 画布、短剧节点、连线即依赖。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 25.3 缺口

通用工作流（非短剧）节点少；无调度器把画布当 cron DAG。

### 25.4 方案

把 scheduler 与画布打通：到点执行子图。通用节点：HTTP、文件、LLM、人工审批。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 25.5 如何算做完

子图重跑正确性。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 25.6 风险与不做什么

变成又一个 Airflow，偏离办公文件交付。

### 25.7 建议步骤

1) 最小通用节点 2) 与短剧共用指纹 3) 克制范围。

### 25.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 25.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 25.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 26. 合规、审计与内容水印

### 26.1 趋势背景

企业要不可抵赖；法规要训练数据与生成标记。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 26.2 现状锚点（去年已做）

审计日志、离职回执、部分水印处理意识。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 26.3 缺口

无 WORM 存储；无生成内容 C2PA；审计 1000 条太短。

### 26.4 方案

企业版审计外送 SIEM；可选 C2PA；个人版保持环形。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 26.5 如何算做完

审计完整性抽检。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 26.6 风险与不做什么

过重。

### 26.7 建议步骤

1) 导出 SIEM 2) 条数可配 3) 水印政策说明。

### 26.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 26.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 26.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 27. 开放标准与避免平台锁

### 27.1 趋势背景

技能、MCP、Plugins、会话导出应可迁移。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 27.2 现状锚点（去年已做）

已走 MCP 与 Plugins；会话是 JSON。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 27.3 缺口

无标准会话交换格式；专家定义私有。

### 27.4 方案

导出 OpenAI-compatible transcript 与 Markdown 包；专家 schema 文档化。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 27.5 如何算做完

用户能在另一 Agent 复现上下文的比例。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 27.6 风险与不做什么

标准过多。

### 27.7 建议步骤

1) 导出包 2) schema 3) 导入校验。

### 27.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 27.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 27.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 28. Windows / 国产系统工程

### 28.1 趋势背景

办公用户大量在 Windows。2026 仍要拼路径、杀毒误报、未签名。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 28.2 现状锚点（去年已做）

已有 cmd、Python 探测、Chrome/Edge 路径、删除保护识别 del。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 28.3 缺口

无代码签名；SmartScreen；杀毒误删。

### 28.4 方案

文档与 doctor 继续；考虑社区签名；减少长路径。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 28.5 如何算做完

Windows 首次成功率。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 28.6 风险与不做什么

买证书贵。

### 28.7 建议步骤

1) 安装遥测可选 2) 杀毒白名单说明 3) CI 真 Windows。

### 28.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 28.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 28.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 29. 可访问性与国际化

### 29.1 趋势背景

政府与大企业采购看无障碍；海外用户要 i18n。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 29.2 现状锚点（去年已做）

有 i18n 文件与主题字号。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 29.3 缺口

未做完整屏幕阅读器；i18n 覆盖不全。

### 29.4 方案

关键流程 ARIA；补英文帮助；避免用颜色当唯一状态。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 29.5 如何算做完

键盘可完成主路径。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 29.6 风险与不做什么

无。

### 29.7 建议步骤

1) 审计 2) 补翻译 3) 测试。

### 29.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 29.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 29.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 30. 供应链与发布工程

### 30.1 趋势背景

npm 依赖攻击是 Agent 项目高危（它会跑代码）。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 30.2 现状锚点（去年已做）

check-package-files、lockfile、CI test/release。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 30.3 缺口

无签名 npm；依赖机器人策略需写明。

### 30.4 方案

锁定引擎；审计高危包；发布 sbom。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 30.5 如何算做完

未锁定依赖进入发行包的次数 0。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 30.6 风险与不做什么

无。

### 30.7 建议步骤

1) sbom 2) 依赖策略 3) 与现有打包测试结合。

### 30.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 30.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 30.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 31. 教育模式：把仓库当 Agent 教材

### 31.1 趋势背景

大量学生用本仓库学 Agent。2026 教材化是增长点。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 31.2 现状锚点（去年已做）

无构建、注释详细、实现细节文档。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 31.3 缺口

缺分步教程与架构图课纲。

### 31.4 方案

在不改性格前提下加「阅读路径」：从 store.js 三函数读起。本 DESIGN/SOURCE 即教材。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 31.5 如何算做完

新贡献者第一次 PR 时间。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 31.6 风险与不做什么

文档腐烂。

### 31.7 建议步骤

1) 课纲 2) 与代码注释互链 3) 测试当习题。

### 31.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 31.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 31.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 32. 失败经济学：假绿与缺依赖

### 32.1 趋势背景

用户最恨绿灯空文件。行业开始把 verifier 当核心。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 32.2 现状锚点（去年已做）

成果核验、假绿裁定、缺二进制人话、媒体熔断。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 32.3 缺口

覆盖面仍可扩到「PPT 0 页」「视频 0 秒」。

### 32.4 方案

按 MIME 与时长/页数做交付物物理检查。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 32.5 如何算做完

空交付物流出率。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 32.6 风险与不做什么

误杀合法小文件。

### 32.7 建议步骤

1) 物理检查表 2) 白名单空文件类型 3) 测试。

### 32.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 32.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 32.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 33. 移动端办公体验

### 33.1 趋势背景

手机指挥是路线图主线之一。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 33.2 现状锚点（去年已做）

pair、双泳道、IM。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 33.3 缺口

跨机器中继未完成；小屏过程卡；审批按钮。

### 33.4 方案

先做好同机远程的小屏 CSS 与审批；再中继。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 33.5 如何算做完

手机完成「批准并收 PPT」闭环。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 33.6 风险与不做什么

小屏做画布不现实，应降级为列表。

### 33.7 建议步骤

1) 小屏布局 2) 画布只读列表 3) 中继。

### 33.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 33.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 33.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 34. 能源与后台驻留

### 34.1 趋势背景

常驻 Agent 耗电。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 34.2 现状锚点（去年已做）

awake 仅任务中；浏览器闲置关。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 34.3 缺口

无全局能耗档。

### 34.4 方案

「省电档」降低轮询、关闭宠物动画。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 34.5 如何算做完

空闲 CPU。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 34.6 风险与不做什么

省电导致定时任务睡过。

### 34.7 建议步骤

1) 档位 2) 与 scheduler 互斥提示。

### 34.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 34.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 34.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 35. 工具结果作为世界接口的类型系统

### 35.1 趋势背景

把每个工具的输出当类型，减少模型自由文本。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 35.2 现状锚点（去年已做）

input_schema 已有；输出仍多自由文本。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 35.3 缺口

无 output schema；agent 偶发解析自己的句子。

### 35.4 方案

对关键工具加 output_schema 与校验，失败则工具层重试而非让模型猜。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 35.5 如何算做完

输出校验失败率。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 35.6 风险与不做什么

过严。

### 35.7 建议步骤

1) 选 10 个工具试点 2) eval。

### 35.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 35.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 35.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 36. 时间与日历作为一等上下文

### 36.1 趋势背景

办公任务几乎都有「几点前」。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 36.2 现状锚点（去年已做）

envToday、scheduler、sleep 顺延。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 36.3 缺口

无用户时区设置完善度；无会议冲突。

### 36.4 方案

用户时区写入 system；与日历只读。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 36.5 如何算做完

定时误差。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 36.6 风险与不做什么

隐私。

### 36.7 建议步骤

1) 时区 2) 只读日历 MCP。

### 36.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 36.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 36.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 37. 多语言任务与术语表

### 37.1 趋势背景

跨国团队要术语锁定。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 37.2 现状锚点（去年已做）

i18n UI；内容语言随用户。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 37.3 缺口

无术语表注入。

### 37.4 方案

项目级 glossary.md 进提示词，Goal 检查禁用词。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 37.5 如何算做完

术语违规。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 37.6 风险与不做什么

无。

### 37.7 建议步骤

1) glossary 2) 断言。

### 37.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 37.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 37.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 38. 数据出境与模型地区

### 38.1 趋势背景

国内办公关心数据送到哪。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 38.2 现状锚点（去年已做）

可配国内渠道与 Ollama。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 38.3 缺口

无按项目强制渠道。

### 38.4 方案

项目锁渠道；尝试出网国外域名时拦。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 38.5 如何算做完

违规出网次数。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 38.6 风险与不做什么

误拦。

### 38.7 建议步骤

1) 项目锁 2) 域名政策。

### 38.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 38.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 38.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。

## 39. 灾难恢复演练

### 39.1 趋势背景

JSON 文件坏了是真实风险。

对 OpenWorkBuddy 的含义是：用户已经接受「Agent 能交文件」，下一步比较的是**稳不稳、贵不贵、敢不敢交给同事、出了问题能不能追溯**。办公场景不是刷榜，是周报、合同、短剧镜头、定时晨报这些会重复、会花钱、会留下法律责任的动作。因此优化必须能说清验收数字，而不是只换一个更潮的名词。

### 39.2 现状锚点（去年已做）

store 恢复逻辑完善。

这些锚点说明项目没有从零开始追趋势，而是已经用故障驱动的方式做了一部分。继续做时要读对应模块注释（往往记录了为什么不能「更聪明地静默处理」），避免把踩过的坑当新需求做回去。

### 39.3 缺口

用户不知 .bak；无一键演练。

### 39.4 方案

doctor 增加「故意损坏演练」开关仅开发。设置页展示最近一次恢复事件。

落地时保持三个约束：① 无构建入口不变，新 UI 继续经典脚本或最少量 API；② 新持久化仍是人能读的 JSON/Markdown；③ 新自动行为必须可关，默认值偏向少花钱、少出网、少自作主张。

### 39.5 如何算做完

演练成功。

负对照：功能关闭时旧路径完全不变。文档：功能清单与 CHANGELOG 各一条人话。

### 39.6 风险与不做什么

用户误点。

### 39.7 建议步骤

1) UI 提示 2) 文档。

### 39.8 与其他条目的依赖

质量轨道的 ContextPack 是许多条目的前置；组织轨道的 Inbox 依赖统一 Run 状态；创作轨道的 DAG 依赖生成缓存与分镜对象库（二者已存在）。排期上不要平行改 agent 主循环的压缩、死循环、failover 三处而不跑 agent-loop 与 e2e。

### 39.9 对提示词与工具描述的影响

若本条改系统提示词或工具 description，按 CONTRIBUTING 必跑 eval。提示词只写机制能保证的事，不要写「你必须一次成功」这种无法执行的道德条款。与 ask_user 有关的禁令必须同一行写明例外，否则 testPromptNoAskContradiction 应红。

### 39.10 安全审查要点

新增网络、执行、身份能力时更新 docs/安全.md 与安全基线。默认监听与密钥文件权限不得放松。技能/MCP 新安装路径必须进 skill-guard。


## 实施原则（总结）

1. **文件仍是真理**：一切「智能」若不能提高磁盘上文件的正确率，就不做。  
2. **人审高影响**：外发、删、花钱、装技能、进化规则。  
3. **显式优于聪明**：换模型、降级、合分支、清缓存都要用户看得见。  
4. **测试先于故事**：先有负对照，再写 CHANGELOG 形容词。  
5. **性格不改**：零构建、本地优先、Markdown 技能、单进程能跑。

## 分阶段建议（按技术依赖而非日历）

**阶段 A — 巩固**：交付物物理检查、ContextPack 最小版、画布引用检查、Review Inbox API、评测基线补齐。  
**阶段 B — 放大**：DAG 重算、TeamGraph 可选隔离、离线档、OIDC 可选、技能夹具。  
**阶段 C — 连接**：安全中继、SIEM 导出、计算机使用连接器化、SCIM。

每一阶段结束时：`npm test` 全绿；核心改动 eval 不差于基线；文档四件套（功能清单、CHANGELOG、安全、本方案勾选）同步。

## 结语

去年把 Agent 做成了「会交文件的本地同事」。今年行业会用更苛刻的尺子量它：长任务记不记得住、钱花得冤不冤、团队敢不敢用、出事能不能查。OpenWorkBuddy 已有的压缩、裁定、进化、worktree、画布缓存，说明方向正确。下一步不是重写，而是把这些机制连成**可证明的质量系统**，同时把企业能力做成可选项，让一个人的桌面仍然轻。


## 附录甲 趋势对照总表（详细阐述）


### 甲.1 对照条目的执行备忘

本条对应正文第 1 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.2 对照条目的执行备忘

本条对应正文第 2 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.3 对照条目的执行备忘

本条对应正文第 3 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.4 对照条目的执行备忘

本条对应正文第 4 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.5 对照条目的执行备忘

本条对应正文第 5 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.6 对照条目的执行备忘

本条对应正文第 6 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.7 对照条目的执行备忘

本条对应正文第 7 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.8 对照条目的执行备忘

本条对应正文第 8 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.9 对照条目的执行备忘

本条对应正文第 9 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.10 对照条目的执行备忘

本条对应正文第 10 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.11 对照条目的执行备忘

本条对应正文第 11 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.12 对照条目的执行备忘

本条对应正文第 12 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.13 对照条目的执行备忘

本条对应正文第 13 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.14 对照条目的执行备忘

本条对应正文第 14 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.15 对照条目的执行备忘

本条对应正文第 15 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.16 对照条目的执行备忘

本条对应正文第 16 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.17 对照条目的执行备忘

本条对应正文第 17 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.18 对照条目的执行备忘

本条对应正文第 18 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.19 对照条目的执行备忘

本条对应正文第 19 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.20 对照条目的执行备忘

本条对应正文第 20 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.21 对照条目的执行备忘

本条对应正文第 21 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.22 对照条目的执行备忘

本条对应正文第 22 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.23 对照条目的执行备忘

本条对应正文第 23 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.24 对照条目的执行备忘

本条对应正文第 24 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.25 对照条目的执行备忘

本条对应正文第 25 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.26 对照条目的执行备忘

本条对应正文第 26 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.27 对照条目的执行备忘

本条对应正文第 27 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.28 对照条目的执行备忘

本条对应正文第 28 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.29 对照条目的执行备忘

本条对应正文第 29 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.30 对照条目的执行备忘

本条对应正文第 30 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.31 对照条目的执行备忘

本条对应正文第 31 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.32 对照条目的执行备忘

本条对应正文第 32 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.33 对照条目的执行备忘

本条对应正文第 33 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.34 对照条目的执行备忘

本条对应正文第 34 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

### 甲.35 对照条目的执行备忘

本条对应正文第 35 个趋势方向（若正文不足 35 条则视为对该编号主题的补充研究笔记）。去年把产品做成「能干活的本地 Agent」时，行业叙事还停留在工具数量和模型品牌。今年买方和开源社区都改口了：他们问长任务会不会忘约束、会不会在假完成上浪费预算、会不会在同事的机器上越权、会不会在手机上批准一次高危命令、会不会把技能供应链当成 npm 一样乱装。

因此执行备忘固定五句话。第一句，保留性格：无构建、JSON 文件、本地默认、技能即 Markdown。第二句，补齐证据：凡是新智能，都要有失败码、评测题或物理检查，不能只加形容词。第三句，人是闸：外发、删除、花钱、装不可信技能、进化规则生效，必须人点头。第四句，显式：换模型、用缓存、开分身、清对象库，界面都要写明白。第五句，可关：企业能力默认不影响个人双击体验。

落地时的代码落点仍然是少数文件。上下文与循环在 agent.js；工具世界接口在 tools.js；身份与租户在 account.js、rbac.js、org.js、lifecycle.js；媒体与画布在 media-models.js、gen-cache.js、shot-history.js、drama-*.js、app-07-*.js；可观测在 trace.js、task-verdict.js、metrics.js；进化在 evolve.js。不要为了追趋势新建一套并行运行时，那会让 CLI、IM、画布再次漂移，modes.js 曾经就是为了消灭这种漂移才出现的。

验证时至少做三层。零 token 单测钉纯函数；e2e 钉接线；付费 eval 钉任务质量。负对照必须能红：把新闸拆掉，测试应当失败。文档同步功能清单、CHANGELOG、安全基线。若改提示词，走 testPromptNoAskContradiction 一类的「拼装后的真提示词」测试，而不是只测字符串常量是否还在源码某处。

沟通上不要对用户承诺「完全自主无人看管」。办公 Agent 的产品诚实性是：它很能干，所以必须能被打断、能被审计、能被回退。这与 2026 年最成功的编码 Agent 产品是同一哲学，只不过我们的交付物是 PPT 和视频，而不是只是 git diff。

## 附录乙 与主流产品的差异化坚持

对标 Claude Code、Codex、各类云 Agent 时，不要在「云沙箱、托管浏览器、中央模型」上正面碰撞。本项目赢的是：文件在用户磁盘、渠道可换、技能可丢 Markdown、短剧画布与办公工具在同一循环、中文办公场景的闸门文案。优化方案全部是把这些优势变得更稳，而不是变成又一个云 IDE。

对标国内办公套件插件时，不要做成「对话框里生成一段不能下载的伪 PPT」。核验磁盘文件、预览 OOXML、公式用真 Excel 引擎，这些已经是护城河，应继续加物理检查而不是加更多皮肤。

对标短剧专用工具时，打「不重复花钱」和「改一镜不重跑全片」。缓存、对象库、直调、并发上限的保守默认（2）都是成本哲学，后续 DAG 重算必须延续这哲学，而不是为了酷炫一次并发行 8 路视频把账单打爆。

## 附录丙 研发节奏建议

以机制为单位合并，不要以「第 N 轮」为荣。每一机制包含：故障证据或评测缺口、最小实现、负对照、文档人话、默认关闭或保守默认。把 evolve 挖出的信号当选题来源，比从社交媒体抄名词更适合这个仓库的文化。

代码评审清单：是否破坏无构建？是否把 Key 打进日志？是否路径逃逸？是否会话半截工具对？是否提示词自相矛盾？是否测试用了用户真实目录？是否需要 eval？是否 IM/CLI/Web 三入口行为一致？

## 附录丁 给未来的自己

若你在 2027 年读到本文，先跑 npm test 和一条真实办公任务，再决定哪一节过时。趋势文档的腐烂速度高于代码。过时的标志是：正文还在建议做某事，而仓库里已经做完且测试以另一名字钉住。那时请把本节改成「已落地」，把指标写成真实数字，而不是继续用将来时。

## 附录戊.1 质量系统的补充说明

所谓质量系统，不是再写一套监控微服务，而是让每一次失败都变成下次更难失败。假绿裁定已经把「没抛异常」和「干成了」切开；成果核验已经把「嘴上说写了文件」和「磁盘上有字节」切开；自进化已经把「点赞变色」和「真的收了信号」切开。2026 年要补的切开还有几刀：把「有路径」和「路径指向的内容仍是那一版」切开（对象库已经在分镜上做了，要推广到普通文档）；把「测试绿」和「断言打在错误信息上的假绿」切开（负对照文化要保持）；把「管理员」和「能发管理员的人」切开（RBAC 已做，要防止新接口绕过 assignProblem）。

每一刀都对应一类真实用户伤害：交上去的 PPT 其实是上一版；CI 绿了但用户仍踩坑；普通管理员互相罢免。优化方案里的 ContextPack、物理检查、Inbox、DAG，本质上都是继续切开「表面成功」和「可验证成功」。只要坚持这一刀法，趋势名词怎么换都不怕走偏。

对贡献者的具体要求是：新功能若自称完成，必须指出用户用哪一个界面动作能揭穿它的假完成。揭不穿的功能不要合并。文档里把揭穿方法写进帮助的验收段。这样 3SOLUTION.md 才不会变成无法执行的战略口号。

## 附录戊.2 质量系统的补充说明

所谓质量系统，不是再写一套监控微服务，而是让每一次失败都变成下次更难失败。假绿裁定已经把「没抛异常」和「干成了」切开；成果核验已经把「嘴上说写了文件」和「磁盘上有字节」切开；自进化已经把「点赞变色」和「真的收了信号」切开。2026 年要补的切开还有几刀：把「有路径」和「路径指向的内容仍是那一版」切开（对象库已经在分镜上做了，要推广到普通文档）；把「测试绿」和「断言打在错误信息上的假绿」切开（负对照文化要保持）；把「管理员」和「能发管理员的人」切开（RBAC 已做，要防止新接口绕过 assignProblem）。

每一刀都对应一类真实用户伤害：交上去的 PPT 其实是上一版；CI 绿了但用户仍踩坑；普通管理员互相罢免。优化方案里的 ContextPack、物理检查、Inbox、DAG，本质上都是继续切开「表面成功」和「可验证成功」。只要坚持这一刀法，趋势名词怎么换都不怕走偏。

对贡献者的具体要求是：新功能若自称完成，必须指出用户用哪一个界面动作能揭穿它的假完成。揭不穿的功能不要合并。文档里把揭穿方法写进帮助的验收段。这样 3SOLUTION.md 才不会变成无法执行的战略口号。

## 附录戊.3 质量系统的补充说明

所谓质量系统，不是再写一套监控微服务，而是让每一次失败都变成下次更难失败。假绿裁定已经把「没抛异常」和「干成了」切开；成果核验已经把「嘴上说写了文件」和「磁盘上有字节」切开；自进化已经把「点赞变色」和「真的收了信号」切开。2026 年要补的切开还有几刀：把「有路径」和「路径指向的内容仍是那一版」切开（对象库已经在分镜上做了，要推广到普通文档）；把「测试绿」和「断言打在错误信息上的假绿」切开（负对照文化要保持）；把「管理员」和「能发管理员的人」切开（RBAC 已做，要防止新接口绕过 assignProblem）。

每一刀都对应一类真实用户伤害：交上去的 PPT 其实是上一版；CI 绿了但用户仍踩坑；普通管理员互相罢免。优化方案里的 ContextPack、物理检查、Inbox、DAG，本质上都是继续切开「表面成功」和「可验证成功」。只要坚持这一刀法，趋势名词怎么换都不怕走偏。

对贡献者的具体要求是：新功能若自称完成，必须指出用户用哪一个界面动作能揭穿它的假完成。揭不穿的功能不要合并。文档里把揭穿方法写进帮助的验收段。这样 3SOLUTION.md 才不会变成无法执行的战略口号。

## 附录戊.4 质量系统的补充说明

所谓质量系统，不是再写一套监控微服务，而是让每一次失败都变成下次更难失败。假绿裁定已经把「没抛异常」和「干成了」切开；成果核验已经把「嘴上说写了文件」和「磁盘上有字节」切开；自进化已经把「点赞变色」和「真的收了信号」切开。2026 年要补的切开还有几刀：把「有路径」和「路径指向的内容仍是那一版」切开（对象库已经在分镜上做了，要推广到普通文档）；把「测试绿」和「断言打在错误信息上的假绿」切开（负对照文化要保持）；把「管理员」和「能发管理员的人」切开（RBAC 已做，要防止新接口绕过 assignProblem）。

每一刀都对应一类真实用户伤害：交上去的 PPT 其实是上一版；CI 绿了但用户仍踩坑；普通管理员互相罢免。优化方案里的 ContextPack、物理检查、Inbox、DAG，本质上都是继续切开「表面成功」和「可验证成功」。只要坚持这一刀法，趋势名词怎么换都不怕走偏。

对贡献者的具体要求是：新功能若自称完成，必须指出用户用哪一个界面动作能揭穿它的假完成。揭不穿的功能不要合并。文档里把揭穿方法写进帮助的验收段。这样 3SOLUTION.md 才不会变成无法执行的战略口号。

## 附录戊.5 质量系统的补充说明

所谓质量系统，不是再写一套监控微服务，而是让每一次失败都变成下次更难失败。假绿裁定已经把「没抛异常」和「干成了」切开；成果核验已经把「嘴上说写了文件」和「磁盘上有字节」切开；自进化已经把「点赞变色」和「真的收了信号」切开。2026 年要补的切开还有几刀：把「有路径」和「路径指向的内容仍是那一版」切开（对象库已经在分镜上做了，要推广到普通文档）；把「测试绿」和「断言打在错误信息上的假绿」切开（负对照文化要保持）；把「管理员」和「能发管理员的人」切开（RBAC 已做，要防止新接口绕过 assignProblem）。

每一刀都对应一类真实用户伤害：交上去的 PPT 其实是上一版；CI 绿了但用户仍踩坑；普通管理员互相罢免。优化方案里的 ContextPack、物理检查、Inbox、DAG，本质上都是继续切开「表面成功」和「可验证成功」。只要坚持这一刀法，趋势名词怎么换都不怕走偏。

对贡献者的具体要求是：新功能若自称完成，必须指出用户用哪一个界面动作能揭穿它的假完成。揭不穿的功能不要合并。文档里把揭穿方法写进帮助的验收段。这样 3SOLUTION.md 才不会变成无法执行的战略口号。

## 附录戊.6 质量系统的补充说明

所谓质量系统，不是再写一套监控微服务，而是让每一次失败都变成下次更难失败。假绿裁定已经把「没抛异常」和「干成了」切开；成果核验已经把「嘴上说写了文件」和「磁盘上有字节」切开；自进化已经把「点赞变色」和「真的收了信号」切开。2026 年要补的切开还有几刀：把「有路径」和「路径指向的内容仍是那一版」切开（对象库已经在分镜上做了，要推广到普通文档）；把「测试绿」和「断言打在错误信息上的假绿」切开（负对照文化要保持）；把「管理员」和「能发管理员的人」切开（RBAC 已做，要防止新接口绕过 assignProblem）。

每一刀都对应一类真实用户伤害：交上去的 PPT 其实是上一版；CI 绿了但用户仍踩坑；普通管理员互相罢免。优化方案里的 ContextPack、物理检查、Inbox、DAG，本质上都是继续切开「表面成功」和「可验证成功」。只要坚持这一刀法，趋势名词怎么换都不怕走偏。

对贡献者的具体要求是：新功能若自称完成，必须指出用户用哪一个界面动作能揭穿它的假完成。揭不穿的功能不要合并。文档里把揭穿方法写进帮助的验收段。这样 3SOLUTION.md 才不会变成无法执行的战略口号。

## 附录戊.7 质量系统的补充说明

所谓质量系统，不是再写一套监控微服务，而是让每一次失败都变成下次更难失败。假绿裁定已经把「没抛异常」和「干成了」切开；成果核验已经把「嘴上说写了文件」和「磁盘上有字节」切开；自进化已经把「点赞变色」和「真的收了信号」切开。2026 年要补的切开还有几刀：把「有路径」和「路径指向的内容仍是那一版」切开（对象库已经在分镜上做了，要推广到普通文档）；把「测试绿」和「断言打在错误信息上的假绿」切开（负对照文化要保持）；把「管理员」和「能发管理员的人」切开（RBAC 已做，要防止新接口绕过 assignProblem）。

每一刀都对应一类真实用户伤害：交上去的 PPT 其实是上一版；CI 绿了但用户仍踩坑；普通管理员互相罢免。优化方案里的 ContextPack、物理检查、Inbox、DAG，本质上都是继续切开「表面成功」和「可验证成功」。只要坚持这一刀法，趋势名词怎么换都不怕走偏。

对贡献者的具体要求是：新功能若自称完成，必须指出用户用哪一个界面动作能揭穿它的假完成。揭不穿的功能不要合并。文档里把揭穿方法写进帮助的验收段。这样 3SOLUTION.md 才不会变成无法执行的战略口号。

## 附录戊.8 质量系统的补充说明

所谓质量系统，不是再写一套监控微服务，而是让每一次失败都变成下次更难失败。假绿裁定已经把「没抛异常」和「干成了」切开；成果核验已经把「嘴上说写了文件」和「磁盘上有字节」切开；自进化已经把「点赞变色」和「真的收了信号」切开。2026 年要补的切开还有几刀：把「有路径」和「路径指向的内容仍是那一版」切开（对象库已经在分镜上做了，要推广到普通文档）；把「测试绿」和「断言打在错误信息上的假绿」切开（负对照文化要保持）；把「管理员」和「能发管理员的人」切开（RBAC 已做，要防止新接口绕过 assignProblem）。

每一刀都对应一类真实用户伤害：交上去的 PPT 其实是上一版；CI 绿了但用户仍踩坑；普通管理员互相罢免。优化方案里的 ContextPack、物理检查、Inbox、DAG，本质上都是继续切开「表面成功」和「可验证成功」。只要坚持这一刀法，趋势名词怎么换都不怕走偏。

对贡献者的具体要求是：新功能若自称完成，必须指出用户用哪一个界面动作能揭穿它的假完成。揭不穿的功能不要合并。文档里把揭穿方法写进帮助的验收段。这样 3SOLUTION.md 才不会变成无法执行的战略口号。

## 附录戊.9 质量系统的补充说明

所谓质量系统，不是再写一套监控微服务，而是让每一次失败都变成下次更难失败。假绿裁定已经把「没抛异常」和「干成了」切开；成果核验已经把「嘴上说写了文件」和「磁盘上有字节」切开；自进化已经把「点赞变色」和「真的收了信号」切开。2026 年要补的切开还有几刀：把「有路径」和「路径指向的内容仍是那一版」切开（对象库已经在分镜上做了，要推广到普通文档）；把「测试绿」和「断言打在错误信息上的假绿」切开（负对照文化要保持）；把「管理员」和「能发管理员的人」切开（RBAC 已做，要防止新接口绕过 assignProblem）。

每一刀都对应一类真实用户伤害：交上去的 PPT 其实是上一版；CI 绿了但用户仍踩坑；普通管理员互相罢免。优化方案里的 ContextPack、物理检查、Inbox、DAG，本质上都是继续切开「表面成功」和「可验证成功」。只要坚持这一刀法，趋势名词怎么换都不怕走偏。

对贡献者的具体要求是：新功能若自称完成，必须指出用户用哪一个界面动作能揭穿它的假完成。揭不穿的功能不要合并。文档里把揭穿方法写进帮助的验收段。这样 3SOLUTION.md 才不会变成无法执行的战略口号。

## 附录戊.10 质量系统的补充说明

所谓质量系统，不是再写一套监控微服务，而是让每一次失败都变成下次更难失败。假绿裁定已经把「没抛异常」和「干成了」切开；成果核验已经把「嘴上说写了文件」和「磁盘上有字节」切开；自进化已经把「点赞变色」和「真的收了信号」切开。2026 年要补的切开还有几刀：把「有路径」和「路径指向的内容仍是那一版」切开（对象库已经在分镜上做了，要推广到普通文档）；把「测试绿」和「断言打在错误信息上的假绿」切开（负对照文化要保持）；把「管理员」和「能发管理员的人」切开（RBAC 已做，要防止新接口绕过 assignProblem）。

每一刀都对应一类真实用户伤害：交上去的 PPT 其实是上一版；CI 绿了但用户仍踩坑；普通管理员互相罢免。优化方案里的 ContextPack、物理检查、Inbox、DAG，本质上都是继续切开「表面成功」和「可验证成功」。只要坚持这一刀法，趋势名词怎么换都不怕走偏。

对贡献者的具体要求是：新功能若自称完成，必须指出用户用哪一个界面动作能揭穿它的假完成。揭不穿的功能不要合并。文档里把揭穿方法写进帮助的验收段。这样 3SOLUTION.md 才不会变成无法执行的战略口号。

## 附录戊.11 质量系统的补充说明

所谓质量系统，不是再写一套监控微服务，而是让每一次失败都变成下次更难失败。假绿裁定已经把「没抛异常」和「干成了」切开；成果核验已经把「嘴上说写了文件」和「磁盘上有字节」切开；自进化已经把「点赞变色」和「真的收了信号」切开。2026 年要补的切开还有几刀：把「有路径」和「路径指向的内容仍是那一版」切开（对象库已经在分镜上做了，要推广到普通文档）；把「测试绿」和「断言打在错误信息上的假绿」切开（负对照文化要保持）；把「管理员」和「能发管理员的人」切开（RBAC 已做，要防止新接口绕过 assignProblem）。

每一刀都对应一类真实用户伤害：交上去的 PPT 其实是上一版；CI 绿了但用户仍踩坑；普通管理员互相罢免。优化方案里的 ContextPack、物理检查、Inbox、DAG，本质上都是继续切开「表面成功」和「可验证成功」。只要坚持这一刀法，趋势名词怎么换都不怕走偏。

对贡献者的具体要求是：新功能若自称完成，必须指出用户用哪一个界面动作能揭穿它的假完成。揭不穿的功能不要合并。文档里把揭穿方法写进帮助的验收段。这样 3SOLUTION.md 才不会变成无法执行的战略口号。


## 附录己 收束

把今年的优化写成一句人话：让每一次「看起来做成了」都更难骗过人和机器。上下文要记得住约束，目标要验得了清单，并行要分得清文件，远程要批得了高危，费用要算得清缓存，技能要扫得了供应链。这些都不要求重写引擎，只要求把已经存在的闸门、裁定、对象库、评测和进化连成闭环。完成的标志不是架构图更好看，而是用户少交一份空文件、少付一笔重复的视频费、少在离职后留下一个还能跑命令的活口。


若只能做三件事：第一件把交付物物理检查（页数、时长、非零字节）接进 Goal 和假绿裁定；第二件把画布下游重算做成指纹 DAG，避免改一镜买全片；第三件把审查收件箱打通网页、终端和 IM，让高影响动作在人点头之前走不出去。三件事都完成后，再谈中继、OIDC 和端侧模型。顺序颠倒会把个人版做重，也会让真正伤害用户的假完成继续存在。记住产品承诺仍然是那句：给你的是能打开的文件，不是一段聊天记录。趋势可以变，这句话不要变。维护者在每次发布前用自己的周报、一份真 PPT、一条真定时任务和一次真离职演练过一遍，比再写一万字路线图更能保证方向正确。把演练结果写进 CHANGELOG 的数字栏，让用户看见稳，而不是看见忙。
