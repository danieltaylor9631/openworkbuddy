# OpenWorkBuddy 使用说明书

**文档编号**：2HELP.md  
**适用版本**：0.6.8  
**读者**：第一次打开应用的个人用户、把实例放到团队服务器的管理员、用 CLI 把 Agent 接进脚本的工程师  

---

## 1. 整体功能概述

OpenWorkBuddy 是装在你自己电脑上的 AI 办公助理。你用一句话交代任务，它自己规划步骤、调用工具（读文件、上网、写 PPT/Word/Excel、画图、生图生视频、发飞书、排定时任务等），最后把**能打开的文件**放到硬盘上，而不是只给一段聊天记录。

它和普通对话框产品的差别可以记三句：

1. **东西在你手里**：会话、Key、成果默认在 `~/OpenWorkBuddy`（Windows 为用户目录下同名文件夹），默认只监听本机。
2. **模型可以换**：DeepSeek、通义、智谱、Kimi、OpenRouter、Ollama，以及本机已登录的 Claude Code / Codex，界面保存即热生效。
3. **能力可以丢文件进去**：`skills/<名字>/skill.md` 存盘后下一条任务就生效，不用重启。

典型一句话任务：

| 你说 | 你得到 |
|---|---|
| 用这份 Excel 做 Q3 复盘 PPT | `.pptx`，成果面板可预览 |
| 调研国内 AI 陪伴产品出报告 | Markdown 或 Word，带来源 |
| 做成手机能看的网页 | HTML + 本机预览扫码 |
| 每天 9 点行业晨报发飞书 | 定时任务 + IM |
| 这条短剧改第 3 镜台词 | 只重跑受影响镜头，旧版首帧可恢复 |

四种干活方式（输入框旁切换）：

- **Ask**：只问答，不改文件。适合先把材料看明白。
- **Plan**：只出计划，不动手。
- **Craft**：完整执行并交付（默认）。
- **Goal**：先拆可验收标准，跑完逐条核对，没达标最多再跑 3 轮。

四种权限（另一颗按钮）：

- **只看不动**：只读。
- **每步都问**：写文件和命令都要你点头，审批条上是 diff 不是文件名。
- **自动改文件**：工作区内随便改，危险命令仍问。
- **全自动**：命令也不问，仍拦文件黑名单和极高危模式。

## 2. 安装与第一次运行

### 2.1 三分钟三条路

**macOS 推荐：**

```bash
curl -fsSL https://raw.githubusercontent.com/CatCatUncle/openworkbuddy/main/install-mac.sh | bash
```

浏览器下的 dmg 会被打隔离标记，curl 下的不会。已手动拖进「应用程序」的，执行：

`xattr -dr com.apple.quarantine /Applications/OpenWorkBuddy.app`

macOS 15 起右键「打开」不再出现第二下「打开」，请用上面命令或「系统设置 → 隐私与安全性 → 仍要打开」。

**Windows：** Releases 下载 `-win-setup.exe`。SmartScreen 点「更多信息 → 仍要运行」。公司电脑禁止安装则用 portable。免安装版第一次可能白屏数分钟，看任务管理器有进程即在解压/编译原生依赖。

**源码：** Node 18+。

```bash
git clone https://github.com/CatCatUncle/openworkbuddy.git
cd openworkbuddy && npm install
npm run app    # 桌面
npm start      # 浏览器 http://localhost:3800
```

起来后在向导或「设置 → 模型」粘一个 API Key。然后直接说人话，例如「做一份介绍 OpenWorkBuddy 的 PPT」。

### 2.2 数据在哪

| 系统 | 目录 |
|---|---|
| macOS / Linux | `~/OpenWorkBuddy` |
| Windows | `C:\Users\你\OpenWorkBuddy` |
| 自定义 | 环境变量 `OPENWORKBUDDY_HOME` |
| Docker | `./openworkbuddy-data` |

卸载**不会**删这个目录。搬家就是拷走整个文件夹。里面有 config.json（Key）、会话、成果、技能、记忆。不要把 config.json 提交到 git。

### 2.3 双击没反应

先看 `~/OpenWorkBuddy/logs/boot.log`。源码跑则 `node cli.js doctor`（或 `openworkbuddy doctor`）。退出码 1 表示有要修的项，报告不包含 Key，可整段贴 issue。

## 3. 界面地图

- **左侧**：任务列表、办公/工程泳道、专家·技能·连接器、自动化（定时任务）、评测、更多。
- **中间**：对话、过程卡（每步耗时、diff、来源链接）、目标勾选卡、流式 SVG。
- **右侧**：本对话成果文件（按今天/昨天/7 天/更早分段）、预览、整理文件夹。
- **输入条**：模式、权限、模型、附件（粘贴截图、拖拽、@路径）、发送。
- **右上**：外观（四档字号、五主题）、头像菜单（账号、管理后台、备份）。

刷新、断网、电脑睡醒会自动接回直播。后台完成或等待审批有系统通知；桌面宠物在主窗口被挡住时用跳跃提醒你「有问题要问」。


## 4. 每一项功能的详细使用说明

### 4.1 对话与插队

新开任务在左侧加号。任务跑着时再发消息默认插入当前任务（改需求、补一句约束）。不要另开一条重复描述，否则会两个 Agent 同时改同一批文件——若工作区是 git 仓库，后到的会进 worktree 分身，你的眼前工作区不动，合不合并你决定。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.2 上传与粘贴

⌘V 截图落入工作区，重名自动编号。超过约 2000 字的粘贴会存成文本文件再挂芯片，避免每一步重发巨长正文。先传文件再说话时，文件会在会话目录生成后自动搬进「本对话」文件夹。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.3 看图

把图当附件或工作区文件，让它「看看这张图里牌子上的字对不对」。内部用 look_at_image，图不进历史，避免上下文爆炸和纯文本模型 400 废会话。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.4 Ask 模式

适合：总结、对比、解释报错、读资料。保证不写盘、不跑会改系统的命令。仍可能上网读公开网页（受 URL 名单约束）。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.5 Plan 模式

只产出步骤清单和风险，不调用写文件工具。可把计划复制后再切 Craft 执行。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.6 Craft 模式

默认。允许写文件、生成办公文档、按权限跑命令。长任务请让它维护 PROGRESS.md，便于续跑。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.7 Goal 模式

说目标即可，如「14 个市州一个都不能少的湖南旅游站」。它拆验收清单，机器检查 JS/JSON/HTML，可选 Jev 判断模型按确定度打勾。没达标自动补跑最多 3 轮。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.8 选模型

输入条模型按钮按对话生效。可开「新对话沿用上次」。菜单显示近 N 次成功率，连挂 ≥2 标红。思考档在设置里：auto 不传参数，off/low/medium/high 映射各家字段。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.9 备用渠道

设置 → 智能体 → failover。只在超时或服务繁忙时换一次。不配置则如实报错，绝不悄悄降级。界面会出现换道事件。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.10 成果面板

本对话文件夹置顶。点文件应用内预览：文本、图片、音视频、docx/xlsx/pptx/zip/csv。不支持的给「用系统程序打开 / 打开所在位置」。大文件只取头 512KB。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.11 整理文件夹

任务结束后可清中间帧、临时音轨、一次性脚本。每组写明为什么敢删，成片和报告不碰。路径逃逸一律拒。删是真删。也可只整理某一个任务。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.12 检查点回退

过程卡上的 diff 可退到某步之前。退错了再退一次等于撤销。任务还在跑时不能退。命令行 `/rewind`。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.13 项目

设置或侧栏创建项目：独立工作目录、项目指令、挂载专家/技能/MCP。目录里若有 AGENTS.md 或 CLAUDE.md 会自动进提示词（截断到约 6000 字）。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.14 资料库

把参考资料丢进资料库，agent 用 library_list/read 自取，不必每次粘贴。支持文件夹、笔记、预览。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.15 记忆

设置里可编辑手写记忆（全局）。Agent 会 remember 用户偏好。不要让它记密码。可导出 Markdown，可从 Claude Code / Codex / Cowork 扫描导入。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.16 专家

广场点专家，或在对话里让协调者委派。专家共享工作目录。专家团按名单接力，适合调研→成稿→PPT 流水线。可自建专家：头像、技能、系统提示词。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.17 技能

内置三十余个。自己写 skills/名字/skill.md。装别人的会先体检：直接装 / 看一眼 / 默认不装。10 类危险默认拦，管理员可强装但留日志。先自己读一遍 Markdown。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.18 MCP 连接器

广场分类点一下。缺命令会提示。stdio 或 HTTP。你的 Key 只作为本机环境变量注入，体检时值打码。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.19 插件

粘 GitHub 地址安装 Agent Plugins 包，同时带技能和 MCP。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.20 无限画布

左侧进入。拖拽平移，Shift 拖拽框选，Shift/⌘ 点选，⌘A 全选，Delete 删除（焦点须在画布）。底部对话框 @ 引用节点。连线表示下一步生成会读的角色/首帧/声音。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.21 短剧工作流

剧本→角色定妆→场景→分镜首帧→图生视频→配音→时间线拼片。改一镜只重算该镜。版本按钮恢复上一版整镜。生成有内容缓存，参数完全一致不二次扣费。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.22 直调重跑一格

画布上重跑镜头走 /api/tool/run，不经过主模型，避免它改写提示词。仅生图/视频/配音/网页截图可直调。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.23 定时任务

自动化页或对 Agent 说「以后每天早上…」。cron 与一次性 at 不要填混：一次性用 at=`+5m`/`18:00`/`2026-09-19 14:05`；每天用 `0 9 * * *`。每次改动要你点头。错过可补跑。结果可推 IM。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.24 IM 飞书

填 app_id/secret/verification_token。群里 @ 机器人即下任务，过程可用交互卡片，完成推回。亦可扫码。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.25 IM 其他

企微/钉钉机器人 Webhook；微信 iLink 或公众号；QQ；通用 Webhook（改默认 secret）。手机不在电脑前也能收结果。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.26 发邮件

配 SMTP。收件人必须在白名单。每封出门前你要看全文。Agent 不会悄悄群发。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.27 本机网页预览

做完 HTML 点预览：相对路径、fetch、localStorage 都是真的。可放开给同一 Wi-Fi 手机（注意绑定地址风险）。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.28 CLI 单发

`openworkbuddy "写周报"` 跑完退出。管道 `cat err.log | openworkbuddy "这是什么问题"`。`-q` 安静只出正文。退出码 0 成功 1 失败 2 参数 130 中断。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.29 CLI 交互

`openworkbuddy` 进 REPL，输入 `/` 看命令：sessions、resume、mode、perm、rewind、mcp、engines 等。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.30 CLI 引擎

`openworkbuddy engines use claude-code` 用已付费订阅跑，不烧 API。权限档仍会翻译成对方 CLI 的开关。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.31 doctor

`openworkbuddy doctor` 查 Node、依赖、目录、配置、模型、端口、ffmpeg 等。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.32 配对手机

`openworkbuddy pair` 出二维码，手机浏览器接管办公/工程泳道。远程访问两个开关默认关。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.33 worktree

`openworkbuddy worktree` 查看分身。合回需你自己 git merge，过程卡会写出命令。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.34 评测

更多 → 评测。分层题，可 repeat k 次看稳不稳。可设基线对比退步。命令行 `npm run eval`。评测写到独立目录，不动你的记忆和额度。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.35 自进化

回复下 👍👎（👎可空理由）。设置 → 自进化 看信号、提案、生效规则。采纳才进提示词。夜间复盘默认关，因为花钱。规则没降失败率会建议下架。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.36 安全中心

看审计、改黑白名单、删文件保护、清空本会话放行。URL 白名单非空则只许名单内域名。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.37 备份

设置里打包 data/config/schedules/experts。恢复前自动 before-restore。恢复后桌面版可重启生效。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.38 外观

字号四档、主题、密度。无障碍方面保持系统焦点环与 contrast token。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.39 桌面宠物

默认没有。说「把这张图做成桌面宠物」并传图，或在设置选 Petdex 精灵。点宠物开关主窗口；有提问会跳；可勿扰。仅 `npm run app`。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.40 管理后台

头像 → 企业管理。建组织、部门模板加人、席位、用量、渠道 Key、安全策略。第一个注册的是平台超管。之后应关闭开放注册。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.41 离职

办理离职一次关：账号、令牌含扫码设备、未用邀请码、2FA、在跑任务、定时任务（交接后停用）。不删对话和文件。出回执。不能给自己办，不能办走最后一个管理员。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.42 额度

可按人设月积分；搜索/生图/视频/TTS/STT 可按次。缓存命中约按十分之一计。超限任务收尾不再续跑。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.43 中转站

给外部客户端发虚拟 Key，按模型与月预算限制，可限 IP。后台可看调用。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.44 Trace

本机执行账本可在界面筛选。接 Langfuse 需显式打开。不要把含隐私的 trace 发到别人的云。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。

### 4.45 更新

桌面会查 GitHub Releases。未签名包更新方式见安装章。

**建议操作顺序**：先确认模式和权限档位与当前任务匹配，再发送。跑起来后看过程卡是否在调你期望的工具；若方向偏了，直接插话而不是停掉重开，除非你确实要另一条独立任务。完成后在成果面板打开文件做人工验收——Goal 模式的勾选是辅助，最终以你能打开的文件为准。若失败，先看过程卡红字和假绿裁定给的下一步（换 Key、等限流、开续跑、装 ffmpeg），再决定是否重跑。重跑生成类镜头前看缓存是否命中；要故意换一版就改提示词或勾 no_cache。


## 5. 配置说明（完整）

配置可在「设置」热更新，也可手改 `config.json`（保存后热加载；坏了回退 `.bak`）。

### 5.1 模型渠道

设置 → 模型 → 选预设（OpenAI / Anthropic / OpenRouter / 火山 / 百炼 / DeepSeek / 智谱 / Kimi / Ollama）→ 粘 Key → 保存。可同一 Key 多型号。`extra_body` 透传私有参数。自建网关用「新渠道」，同地址两条不会被合并（两个账号两把 Key）。

对话、生图、生视频、TTS、STT、看图各表各配。视频五家：通义万相、Seedance、CogVideoX、海螺、硅基流动。认不出协议不会发请求，避免按条计费空等。

### 5.2 Agent 预算

见 config.example.json 注释：步数、时长、续跑轮数、工具/模型超时、生成并发、思考档、token 总预算、压缩保留字符。续跑会花钱，默认 0。

### 5.3 搜索

`search.provider=jina` 配 Jina Key；否则免费 DuckDuckGo。深度研究依赖搜索+抓页，失败时它应尝试数据接口、本机 CLI、转载页，而不是只说抓不到。

### 5.4 安全

permission_mode、文件黑名单、命令询问名单、删除保护、URL 白名单、审计。生产公网务必：HTTPS、先注册管理员、关开放注册、改 webhook_secret、限制监听、打开 2FA。

### 5.5 IM 与通知

飞书应用需开通机器人与发文档权限。企微/钉钉用群机器人 URL。微信按官方开放接口，不搞逆向。通知工具只能推你配过的群机器人。

### 5.6 SMTP

host/port/user/pass + 收件人白名单。未配则 send_email 工具不可用。

### 5.7 MCP

```json
"mcp_servers": [{ "name": "filesystem", "command": "npx", "args": ["-y", "@modelcontextprotocol/server-filesystem", "/某目录"] }]
```

### 5.8 Langfuse

enabled 默认 false。self-host 才建议开。公钥私钥在 Langfuse 项目设置。

### 5.9 环境变量

`OPENWORKBUDDY_HOME`、`OPENWORKBUDDY_DATA_DIR`、`ANTHROPIC_API_KEY`、`OPENAI_API_KEY`、`TYPESAFE_API_KEY`、`OPENWORKBUDDY_TOOLWARD=off`（测试用）。密码重置：`openworkbuddy passwd`；转让平台超管：`openworkbuddy owner <用户名>`（能读 users.json 的人已是机主）。

### 5.10 Docker

`bash deploy.sh --domain buddy.example.com`。反代示例见 deploy/Caddyfile 与 nginx.conf。健康检查通过才算成功。升级：拉代码，再跑脚本；数据目录不动。

## 6. 内置技能使用要点

### 6.1 `deep-research`

深度调研：多轮检索、交叉验证、出带证据链的研究报告。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.2 `html-page`

网页生成：单文件或小站点，可本机预览扫码。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.3 `web-styles`

网页视觉方向：动笔前先挑一种风格，避免每次从零审美。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.4 `ppt-design`

PPT：用 pptxgenjs 真生成 .pptx，可直接放映。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.5 `docx`

Word：官方级 OOXML 工具链，含修订、批注、样式。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.6 `excel-report`

Excel 报表：公式驱动汇总，口径写在表头。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.7 `data-viz`

数据可视化：走 gen_diagram，不让模型手搓歪 SVG。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.8 `weekly-report`

周报：从材料归纳本周进展、风险、下周计划。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.9 `wechat-article`

公众号推文：排版主题 + 推草稿箱脚本。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.10 `xhs-cards`

小红书图文卡片：3:4 HTML 排版后真浏览器截图。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.11 `xiaohongshu-topic`

小红书选题：八大梦境法。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.12 `video-compose`

图文成片：配音 + 字幕 + ffmpeg 拼竖屏。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.13 `short-drama`

AI 短剧：分镜表到定妆、首帧、图生视频、配音、拼片，改一镜只重算那一镜。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.14 `feishu-doc`

飞书文档：官方 convert API，原生表格加粗插图。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.15 `lark-cli`

飞书全家桶命令行。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.16 `skill-creator`

让 agent 自己写技能。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.17 `meeting-minutes`

会议纪要：录音转写再成稿。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.18 `contract-review`

合同风险清单。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.19 `email-draft`

商务邮件。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.20 `project-plan`

项目排期含关键路径。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.21 `prd`

需求文档。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.22 `recruiting`

招聘：JD / 筛简历 / 结构化面试题。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.23 `support-scripts`

客服话术。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.24 `financial-model`

财务测算。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.25 `infographic`

信息图。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.26 `market-research`

市场调研。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.27 `customer-feedback`

用户反馈归纳。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.28 `seo-brief`

SEO 简报。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.29 `sales-outreach`

销售开发信。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.30 `data-analysis`

数据分析。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.31 `content-studio`

内容工作室。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.32 `content-repurpose`

一稿多投。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.33 `competitor-watch`

竞品监控。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.34 `character-photo-studio`

角色定妆照工作室。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。

### 6.35 `podcast`

播客脚本与音频。

使用：在对话中直接提出对应交付物，或先在广场启用。协调者应 `use_skill` 加载后严格按技能流程，而不是凭记忆简化步骤。若技能要求本机二进制（ffmpeg、libreoffice、plantuml），先 `openworkbuddy doctor` 看缺什么。自定义技能用 skill-creator 生成骨架，再人工删掉任何让它读私钥或 curl|bash 的句子，安装扫描才能过。


## 7. 内置工具（用户侧理解）

你通常不必直接点工具，但看过程卡时需要对上号：

- **run_node**：在隔离的 Node 环境跑一段脚本，写后可做语法预检。
- **run_shell**：按平台挑 shell 执行命令；macOS zsh 开 nonomatch；高危命令走审批。
- **write_file**：写入工作区内文件，落盘前留检查点，写后按扩展名自检。
- **edit_file**：按旧文本替换，目录冲突报人话，内容没变不记检查点。
- **read_file**：读文本或按范围读大文件。
- **read_document**：读 Office 文档结构化内容。
- **search_files**：工作区内按关键字搜文件。
- **list_files**：列目录。
- **remember**：写入长期记忆条目，拒密钥与过期能力断言。
- **forget**：删除记忆条目。
- **check_page**：用隐藏窗口打开本地页并收集控制台，滤掉 Electron 自注入噪声。
- **save_skill**：把一段技能 Markdown 存成 skills/<名>/skill.md。
- **library_list**：列资料库。
- **library_read**：读资料库文件。
- **library_import**：把外部材料导入资料库。
- **fetch_url**：抓网页：浏览器头、编码识别、空壳则渲染兜底。
- **chrome_cdp**：通过 CDP 控制真浏览器。
- **render_page**：用内置 Chrome 渲染动态页再取正文。
- **web_search**：Jina 或 DuckDuckGo 搜索。
- **gen_diagram**：mermaid/dot/echarts/plantuml 离线出 SVG+PNG。
- **look_at_image**：带着问题看图，只把文字答案带回，图不进历史。
- **generate_image**：文生图/参考图，内容寻址缓存，5xx 重试。
- **generate_video**：五家视频协议，首尾帧校验，未识别协议拒发。
- **html_to_image**：HTML 离屏截 PNG。
- **text_to_speech**：OpenAI 兼容或 DashScope 配音。
- **transcribe_audio**：音频转写。
- **desktop_pet**：用用户照片做桌面宠物。
- **canvas_manage**：增删改画布节点与连线。
- **delegate_to_expert**：委派单个专家子代理，共享工作目录。
- **delegate_to_team**：按专家团名单顺序接力。
- **ask_user**：弹出选项卡等待用户，超时走默认。
- **feishu_doc_create**：Markdown 转飞书云文档，可等权限。
- **notify_user**：推企业微信/钉钉群机器人。
- **schedule_task**：增删改定时任务，每次都要用户点头。
- **list_schedules**：列出定时任务。
- **send_email**：SMTP 发信，白名单硬闸，出门前预览。
- **use_skill**：加载某个技能 Markdown 进当前上下文。

## 8. 常见问题 100 问

### 8.1 打不开 mac 应用怎么办？

用 install-mac.sh 或 xattr 去掉隔离。macOS 15 不要指望右键第二次打开。看 boot.log。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.2 Windows SmartScreen 拦截？

更多信息 → 仍要运行。项目无商业代码签名证书。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.3 双击后长时间白屏？

portable 首次会解压。源码版曾因 /api/info 401 被当成没起来，现已改为任意 HTTP 即就绪；若仍慢看 boot.log 分段计时。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.4 端口 3800 被占用？

改 config server.port 或关掉占用进程。doctor 会报。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.5 npm start 后浏览器打不开？

确认监听 127.0.0.1:端口。公司代理可能劫持 localhost。试 curl /api/ping。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.6 必须联网吗？

配 Ollama 可本地对话。但技能若要搜索、生图、视频则仍要对应网络。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.7 Key 填在哪？

设置 → 模型，或 config.json。不要提交仓库，不要发到 IM。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.8 Key 格式错误如何发现？

保存时本地形态校验，不必等 401。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.9 DeepSeek 工具调用没执行却开始编造网页内容？

中转把 tool token 解进正文。本项目会还原调用；请用较新版本。仍失败则换不吞 tool_calls 的中转。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.10 如何用 Claude Code 当引擎？

本机装好并登录后，设置选引擎或 `engines use claude-code`。权限档会翻译过去。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.11 Ask 模式为什么还能上网？

读网页是只读。若不希望，把权限与 URL 名单收紧，或断开网络。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.12 如何完全禁止写盘？

权限选「只看不动」。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.13 每步都问太烦？

改「自动改文件」。删除、sudo、高危模式仍会问。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.14 全自动安全吗？

仍拦黑名单与 DANGER_PATTERNS。公网多用户不要默认全自动。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.15 它把我的 SSH 私钥读走了吗？

黑名单默认拦 ~/.ssh 等。审计里可查拦截。仍建议不要把工作区指到家目录根。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.16 误删文件能找回吗？

write/edit 的检查点可 rewind。sweep 删除是真删。整理重复文件若走 .trash 可捞。分镜对象库有版本。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.17 两个任务改同一仓库把我的代码搞乱？

后到的进 worktree，你的工作区应不动。用 `worktree` 查看，按过程卡命令自行合并。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.18 任务做到一半崩溃？

会话已阶段性落盘。重启后可接着说「继续」。running.json 会标中断。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.19 到步数上限就停？

设置自动续跑轮数，并确保有 PROGRESS.md。手动停止不续。token 预算用尽不续。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.20 电脑合盖任务失败？

睡眠治理会顺延时限并重试当步。仍建议电源设置防止深睡，或用服务器部署跑定时任务。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.21 如何让每天自动跑？

建定时任务，机器要在且进程要在。服务器 Docker 更合适。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.22 cron 写成一次性提醒却每天响？

一次性用 at 字段。`0 14 * * *` 是每天 14 点。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.23 定时任务谁能改？

每次增删改要当前用户批准。定时任务自己不能再改排期表。离职交接后默认停用。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.24 飞书收不到？

查 verification_token、权限、是否 @正确。看审计与 IM 日志。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.25 微信怎么接？

走官方 iLink 或公众号接口配置，不要用非官方协议。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.26 手机如何连家里电脑？

pair 扫码。远程开关默认关，打开前先懂风险。跨机器中继尚未做完。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.27 如何换电脑？

拷贝整个 OpenWorkBuddy 目录。Key 在 config.json 里一起走。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.28 如何备份？

界面备份或拷目录。恢复前有安全备份。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.29 记忆会串号吗？

条目按账号隔离。手写区是全局，别写别人的隐私。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.30 为什么记不住我说过的偏好？

应出现 remember。可手写到记忆区。召回在超长提示时才筛选，可配 embedding。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.31 能不能记密码？

不能，模式匹配会拒记。不要在对话里贴 Key。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.32 👍👎有用吗？

旧版只变色；现版会 POST 反馈并进入自进化。👎理由选填。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.33 自进化会不会自己改提示词把系统搞乱？

提案必须人审。代码类问题不许写成提示词规则。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.34 评测会不会花我的钱？

离线题库测试不调模型。真跑 eval 会调你配置的模型，但数据目录隔离。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.35 如何知道哪家模型更稳？

模型菜单健康账本；评测基线；Trace 失败原因。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.36 生图总失败？

看是否 5xx（会重试）、额度、水印、型号是否匹配参考图。媒体熔断会暂停坏渠道。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.37 视频怎么配？

选对厂商协议和型号。图生要传首帧。不支持尾帧的不要填。认不准协议不会发。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.38 为什么重跑镜头还是很快？

生成缓存命中。要新版本请改参数或 no_cache。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.39 改台词后旧首帧没了？

用镜头版本恢复。恢复的是整镜不是单文件路径。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.40 画布 Delete 没反应？

先点画布让焦点过去，或用工具条框选后再删。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.41 图表只有 SVG 没有 PNG？

新版本等渲染进程退出策略已修。飞书要 PNG，gen_diagram 应出两份。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.42 中文图表豆腐块？

Graphviz 设中文字体；导出 PNG 会内联字体。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.43 抓 B 站微博失败？

已带浏览器头。动态页用渲染。仍风控则换登录态 CDP 或官方接口。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.44 页面明明有内容却说没有？

SPA 需 render_page。check_page 假阳性已滤 Electron 警告。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.45 Excel 公式是死数字？

技能要求用 SUM 等公式。若不是，在 Goal 或插话要求改。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.46 PPT 打不开？

应是真 pptxgenjs 产物。用成果面板预览或系统 PowerPoint/WPS。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.47 Word 样式乱？

走 docx 技能与 OOXML 工具，不要让模型手写 XML。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.48 如何指定输出文件名？

在任务里写清路径。生成类给 filename 才会进缓存键。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.49 根目录一堆散文件？

新版本按对话子目录。可用整理与清重复（哈希相同才进垃圾）。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.50 空对话也建文件夹？

纯聊天收尾会撤空目录。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.51 如何共用模板？

放资料库或项目目录，或做成技能。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.52 MCP 连不上？

看广场缺什么命令。npx 要网络。stdio 进程崩溃看日志。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.53 技能被拦住？

读扫描报告。有反弹 shell、curl|bash、读私钥等会默认不装。管理员强装会留痕。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.54 要不要装 toolward？

可选。装了只更严。没装不影响。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.55 多人如何用一台 VPS？

Docker 部署，先注册超管，关开放注册，按部门模板加人，配席位与额度。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.56 第一个访问的人成了超管？

空实例公网确实如此。起来立刻注册并关注册。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.57 管理员能互踢吗？

不能。只能管比自己低的档。超管每组织一个，只能转让。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.58 超管账号丢了？

服务器上 `openworkbuddy owner 用户名`。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.59 忘了密码？

`openworkbuddy passwd`。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.60 如何强制 2FA？

后台安全策略。TOTP 扫码。窗口 ±30 秒。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.61 审计员能干什么？

进后台看用量和配置，不能改人。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.62 离职会删他的 PPT 吗？

不会。只关权限。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.63 用量不准？

缓存命中单独字段，老流水没有 cached 不算进命中率分母。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.64 如何接 Prometheus？

`/api/ops/metrics.prom`，需平台管理员。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.65 告警怎么推？

指标阈值到企微/钉钉，见运维设置。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.66 CLI 和桌面不是同一份数据？

应是同一 HOME。检查是否一个用了 sudo 导致目录不同。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.67 管道模式为什么不提问？

故意的，避免 cron 卡住。把约束写全。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.68 如何静默只要正文？

`-q` 或 `--json`。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.69 退出码怎么用在 CI？

`openworkbuddy doctor && openworkbuddy -q "..."`。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.70 终端里图片？

term-image 会尝试画出来；`/open` 用系统程序打开。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.71 REPL 命令打错？

会提示最近似命令。`/help`。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.72 如何切换模式权限？

界面按钮或 `/mode` `/perm`。Goal 在 CLI 有壳，不要以为只是个字符串。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.73 Jev 是什么？

判断模型，做是非/单选/打分，带确定度。Goal 验收可用。不是聊天模型，不要放进 models[]。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.74 Ollama 模型名怎么填？

base_url `http://localhost:11434/v1`，model 如 `qwen3:14b`。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.75 OpenRouter 一个 Key 通吃？

可以，模型名用 `厂商/型号`。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.76 思考模式关不掉？

agent.thinking=off。各家字段不同，由 thinking.js 映射。auto 表示不传。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.77 上下文被截了？

界面会写截了多少。重要约束放项目指令或 PROGRESS 或记忆。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.78 它反复同一命令？

死循环硬停 3 次提醒 5 次拦截。把错误信息给它或插话换路。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.79 缺 ffmpeg？

doctor 与工具回执有安装句。拼片技能没有它会失败。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.80 Windows 命令语法？

run_shell 用 cmd。提示词会按平台说明。不要贴 bash 专有语法除非装了 Git Bash 并改配置。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.81 路径斜杠？

工具层会处理。尽量用相对工作区路径。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.82 预览 TypeScript 变成播放器？

已按后缀表而不是 mime。请升级。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.83 预览 XSS？

服务端只吐数据，前端 esc。不要改成服务端拼 HTML。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.84 如何关远程访问？

设置里两个开关默认关。打开即有风险，配合配对令牌。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.85 局域网给同事用？

等于小型服务器，按多人协作文档做账号，不要裸 HTTP 暴露。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.86 License 能商用吗？

PolyForm Noncommercial。公司内部提效需商业授权。授权不解锁功能，代码已全部公开。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.87 技能和插件归谁？

你自己写的是你的。仓库内技能随项目协议；生态示例部分 MIT。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.88 与腾讯 WorkBuddy 关系？

无。独立开源项目。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.89 如何参与开发？

CONTRIBUTING。改提示词或工具描述要跑 eval。npm test 不需 Key。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.90 前端为什么拆多个 app-0x.js？

无构建下降低单文件体积。不要改成乱序加载。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.91 如何改端口被公司防火墙拦？

改 port；或走 SSH 隧道。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.92 日志太大？

Trace 轮转；审计环形；boot.log 可删。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.93 如何关闭桌面宠物？

设置关或右键勿扰。不生成则默认不存在。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.94 通知太多？

系统通知与宠物可关。IM 推送按渠道关。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.95 如何让它不要问选择题？

技术路线它应自己定；关键商业选择应走 ask_user。提示词已区分。你也可以直接插话拍板。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.96 Goal 打勾不准？

机器检查只能查语法/文件存在。质量仍靠人。Jev 确定度不够会标拿不准。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.97 如何固定随机种子式的复现？

生成缓存是确定性复用。评测题有基线。模型本身非确定，temperature 看渠道 extra_body。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.98 代理/镜像？

base_url 指到你的网关。视频协议仍需能识别上游。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.99 内容安全与水印？

上游可能强制水印。不要让 Agent 把「已无水印」写进记忆。设置与渠道文档为准。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.100 如何卸载干净？

卸应用后手动删 OpenWorkBuddy 目录。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.101 升级失败？

数据目录不动，回滚二进制。看 CHANGELOG。迁移 notes 在 /api/migrations。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.102 测试污染我的数据？

npm test 使用临时目录和独立 TRACE_FILE。不要把 OPENWORKBUDDY_HOME 指到生产再跑测试。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.103 如何报告 bug？

GitHub issue，附 doctor 与 boot.log，打码 Key。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。

### 8.104 功能这么多从哪学起？

先完成一次 Craft 出 PPT。再试 Goal、再试一个技能、再配飞书。画布和多租户最后。 若仍失败，把模式、权限、渠道名、是否续跑、过程卡最后三步工具名记下来。不要把 api_key 贴进聊天或 issue。可复制 doctor 全文。同一问题先搜 CHANGELOG 与 docs/安装与启动.md、安全.md、配置模型.md。团队场景优先看 多人协作.md 与 部署.md。画布与短剧看 短剧画布选型.md 与技能 short-drama。命令行细节以 docs/命令行用法.md 为辅，和桌面共用会话。

**相关模块**：该问题通常涉及设置页、过程卡或 `openworkbuddy doctor`。产品设计上倾向于把失败原因写成下一步动作（充值、换渠道、装依赖、改权限），而不是只抛异常栈。若你发现界面绿灯但文件不对，请看假绿裁定与成果核验，这是本产品刻意做的「文件才是真理」。


## 9. 故障排查流程

1. 读 boot.log / 过程卡 / 裁定原因。  
2. doctor。  
3. 查权限档与黑名单是否误拦。  
4. 查渠道健康与熔断、额度。  
5. 查磁盘路径是否在会话目录。  
6. 必要时开 Trace 看原始模型输出（注意隐私）。  
7. 仍不解则最小复现 + 打码日志开 issue。

## 10. 附录：快捷对照

- 数据根：`~/OpenWorkBuddy`  
- 默认端口：3800  
- 测试：`npm test`  
- 版本：package.json 的 version 字段  
- 协议：LICENSE PolyForm-Noncommercial-1.0.0  

使用时请遵守当地法律与上游模型服务条款。Agent 能执行 shell，把机器交给它等于把同等权限交给一段不可完全预测的程序——闸门是给你用的，不是摆设。

## 11. 从零到交付的十条完整操作剧本

下面十条覆盖最常见的真实工作日。每条都写清要点击哪里、Agent 大概会调哪些工具、你如何验收、失败时看什么。不必一次读完，用到哪条翻哪条。

### 11.1 十分钟出一份介绍产品的 PPT

打开应用，确认模式为 Craft、权限为「自动改文件」、模型已填 Key。在输入框说：「做一份 12 页左右的 OpenWorkBuddy 介绍 PPT，面向非技术的办公室同事，少代码多场景，最后一页放 GitHub 地址。」发送后过程卡应出现 use_skill（ppt-design）或直接 pptxgenjs 相关的 run_node。不要在它还在写大纲时插话改成 Word，除非你真改交付物类型。完成后右侧点 `.pptx` 预览：看首页标题是否被切字、图表是否为真图而不是「此处插图」。用系统 PowerPoint 打开一次动画与字体。若只有大纲 md 没有 pptx，看成果核验是否打回；没有打回则插话「必须落到 pptx 文件」。

### 11.2 用已有 Excel 做复盘

先把 Excel 拖进输入条或丢到工作区。说清「数据用这个表，不要编数字，汇总用公式」。它应 read_file 或 read_document，再用 run_node + exceljs。验收：改一个明细数字，汇总是否跟着变。口径（时间范围、单位、剔除项）应写在表头或说明页。脏数据应在说明里写处理方式。

### 11.3 深度调研一篇竞品

Goal 模式：「调研国内三个 AI 办公 Agent，输出对比表和差异化建议，数字必须带来源和时间。」应看到 web_search 与 fetch_url 交替，而不是只搜一次就写。来源列表应是它真正打开过的 URL。单一来源应标待核实。若开始编造链接，停掉并换渠道或要求「只引用工具里出现过的 URL」。

### 11.4 湖南旅游站那类硬约束网页

Goal：「14 个市州一个都不能少」。完成后用本机预览打开，搜索市州名。check_page 若报 Electron 警告可忽略（已过滤）；若报真 JS 错，让它修。最终应能离线打开，不依赖外链 CDN（技能 html-page 的常见要求）。

### 11.5 每天晨报推飞书

先在设置配好飞书并在群里 @ 成功一次。再对 Agent 说「以后每个工作日 9:00 抓科技新闻做 300 字晨报发到飞书」。它应 schedule_task 弹审批。你要核对 cron 是工作日而不是每天，task 字段是自包含指令而不是「接着上面」。到点后自动化 → 运行记录应能点开工具列表。漏跑开补跑。电脑睡眠则改部署到 VPS。

### 11.6 短剧改一句台词

无限画布选中镜头，改台词，重跑该节点。看是否走缓存（没改提示则应命中）或直调 /api/tool/run。旧版点版本恢复，确认视频和首帧一起回去，不要出现「新视频配旧首帧」。缺 ffmpeg 时不要让它空转 25 步，先 doctor 安装。

### 11.7 合同风险清单

丢 PDF/Word 进资料库或附件。权限若全自动，仍建议对「对外发送」保持询问。交付应是风险表：条款原文摘录、风险、建议问法，而不是改合同正文（除非你明确要求出修订稿）。

### 11.8 会议纪要

上传录音。应 transcribe_audio 再 meeting-minutes。验收待办是否有负责人与截止日期。隐私：录音留在你的磁盘，注意备份策略。

### 11.9 从终端接着网页上的任务

网页做到一半，终端 `openworkbuddy sessions` 找到 id，`openworkbuddy resume`。插话两边互通靠 cli-live。不要开第二个 HOME 目录。sudo 跑 CLI 会把数据写到 root 家目录，这是常见「怎么会话丢了」原因。

### 11.10 团队里给新人开号

超管在后台用部门模板加人：角色和月额度留空即用模板。把登录地址、首次改密、2FA 强制策略发给新人。不要共用超管。给演示用的虚拟 Key 限模型和 IP。

## 12. 配置项逐项说明（界面对照）

### 12.1 设置 → 模型

渠道卡片显示探测结果：通、401、超时。重复地址会打标签但不会合并。删除渠道前会统计连坐的对话模型、媒体模型、是否包含默认模型。测活按钮对 Jev 走 decisions 接口，不会用 chat 去撞 400。

### 12.2 设置 → 智能体

步数、时长、续跑、并发、思考、备用渠道、token 预算、压缩保留字符。每一项旁边的说明来自 config.example.json 的同义注释。改这些不必重启。正在跑的任务仍用开始时的预算，除非实现注明热更新（以界面提示为准）。

### 12.3 设置 → 安全

四档权限、命令询问名单、删除保护、文件黑名单、URL 白名单、审计导出、本会话放行清空、toolward 状态。公网务必打开登录与 2FA。

### 12.4 设置 → 通知 / IM

Webhook secret 务必改掉示例。飞书文档权限不足时工具会返回开通链接并可 wait_for_permission。

### 12.5 设置 → 外观 / 宠物

主题与字号只存在 prefs，不进 git。宠物照片只本机。

### 12.6 设置 → 用量 / 额度

看 cached 命中要用「记过该字段」的样本。积分不足时任务会拒或收尾，不会默默欠费跑完。

### 12.7 设置 → 备份 / 记忆 / 缓存 / 自进化

见前文功能节。清缓存会让下次生成重新花钱。清审计不可恢复。下架进化规则是删 Markdown 文件。

## 13. 命令行参数与 REPL 详解

全局风格：短命令给人用，`--json` 给脚本。单发不提问。`--mode` `--perm` `-C` 工作目录 `-f` 附件。

常用子命令：

- 无子命令 + 句子：单发任务
- 无参数：REPL
- `doctor`：体检
- `sessions` / `resume`：会话
- `engines`：列出/切换/测试引擎
- `worktree`：分身
- `pair`：扫码
- `passwd` / `owner`：机主维护
- `jev`：判断模型测活或答题

REPL `/` 菜单：mode、perm、model、rewind、mcp、compact 提示、打开文件、会话检索。粘贴多行有合并时间窗，避免把代码块拆成多次发送。

退出码必须被 CI 当真理：不要 `|| true` 吃掉失败。

## 14. 管理员十七个后台面板怎么用（概念）

后台按 RBAC 显示。典型分组：

1. 总览：在线、任务、费用  
2. 组织与套餐席位  
3. 成员与部门模板  
4. 邀请码  
5. 离职办理  
6. 用量与分模型账单  
7. 渠道与 Key（含自建网关）  
8. 媒体模型  
9. 安全策略与 2FA 强制  
10. 审计  
11. 虚拟 Key 中转  
12. 定时任务跨用户视图（管理员）  
13. 运行日志  
14. Prometheus  
15. 公告或迁移 notes  
16. 技能强装记录  
17. 平台级新建组织（仅平台超管）

操作原则：先看能否管到这个人（outranks），再改角色。转让超管是专门按钮，不是下拉里选 owner。

## 15. IM 渠道对照

| 渠道 | 你需要准备 | 用户怎么用 | 成果怎么回 |
|---|---|---|---|
| 飞书 | 企业自建应用 | @机器人说话 | 卡片+文件 |
| 企微 | 群机器人 URL | 往群里发 | webhook 文本/文件策略以实现为准 |
| 钉钉 | 群机器人 | 同左 | 同左 |
| 微信 | iLink 或公众号配置 | 扫码或公众号 | 媒体上传接口 |
| QQ | 机器人配置 | 私聊/群 | 以实现为准 |
| Webhook | 改 secret | POST | JSON |

入站图片会经 im-media 落盘再当附件。不要在 IM 里发 Key。

## 16. 短剧画布键位与节点含义

- 空白拖拽：平移  
- Shift 拖：框选  
- Shift/⌘ 点：加减选  
- ⌘A：全选  
- Delete/Backspace：删选中（焦点在画布）  
- @：引用节点进对话  

节点种类包括剧本、角色、场景、分镜、音频、成片等（以界面为准）。连线不是装饰：生成时会读上游角色定妆与首帧。进度条展开不要挡住画布，新版本工具条会让位。

## 17. 评测与自进化给「爱折腾的用户」

评测不是游戏分数，是你改提示词后有没有把「会做的题」做坏。设基线、改完再跑、看退步题名。自进化信号排行里标着该改代码还是改提示词——代码问题不要采纳成规则。

## 18. 安全使用守则（必读摘要）

Agent 等于一个会敲命令的同事。工作区不要指到整个硬盘。全自动只给自己的玩具目录。公网必须登录。技能先读再装。邮件白名单。URL 白名单在做开放浏览时再放宽。定期看审计。离职走流程。

## 19. 附录：词汇表

Agent 循环、工具、技能、专家、连接器、MCP、插件、会话、工作区、成果、检查点、worktree、续跑、压缩、Trace、假绿、Goal、Jev、泳道、配对、虚拟 Key、熔断、生成缓存、分镜对象库、RBAC、超管转让、部门模板、强装。

以上词汇在界面出现时含义与本文一致。若某次版本改了文案，以界面为准并应更新文档。

## 20. 附录：一天工作流示例（个人）

9:00 看定时晨报是否进飞书；9:10 Ask 模式读三篇行业文；9:30 Craft 出周报 PPT；10:00 画布改两镜；11:00 终端 doctor 确认 ffmpeg；14:00 把合同丢给 Goal；16:00 给明天排定时任务；17:00 备份一次；17:10 看用量与缓存命中，决定要不要换渠道。

## 21. 附录：一天工作流示例（管理员）

看 ops 指标是否告警；看额度是否被异常打满（可能 Key 泄漏或循环）；办一名离职；给设计部门加席位；轮换 webhook secret；抽查强装技能记录；确认证书续期（若有反代）。

## 22.1 外观与辅助功能

字号四档足够投影演示。色盲用户不要只靠红绿看渠道健康，看文字「连挂 2」。屏幕阅读器支持仍有限，关键操作尽量提供按钮文案而不是纯图标。

在实际使用中，这类环境问题往往被误判成「模型不行」。先 doctor、看 boot.log、换一个最小任务（问现在几点、写一个 hello.md）区分是运行时坏了还是任务太难。环境健康之后再加大任务复杂度。权限档可以从每步都问开始，建立信任后再放宽。团队培训时把这一段当作开学第一课。

## 22.2 多显示器

主窗口与宠物分离。审批时宠物会跳，建议把宠物放在常看的屏角。

在实际使用中，这类环境问题往往被误判成「模型不行」。先 doctor、看 boot.log、换一个最小任务（问现在几点、写一个 hello.md）区分是运行时坏了还是任务太难。环境健康之后再加大任务复杂度。权限档可以从每步都问开始，建立信任后再放宽。团队培训时把这一段当作开学第一课。

## 22.3 投影演示

用大字号主题。不要在投影上打开设置页暴露 Key。演示用虚拟 Key 限额度。

在实际使用中，这类环境问题往往被误判成「模型不行」。先 doctor、看 boot.log、换一个最小任务（问现在几点、写一个 hello.md）区分是运行时坏了还是任务太难。环境健康之后再加大任务复杂度。权限档可以从每步都问开始，建立信任后再放宽。团队培训时把这一段当作开学第一课。

## 22.4 教学课堂

clone 后 npm test 给学生看负对照。不要让学生把 Key 提交作业仓库。

在实际使用中，这类环境问题往往被误判成「模型不行」。先 doctor、看 boot.log、换一个最小任务（问现在几点、写一个 hello.md）区分是运行时坏了还是任务太难。环境健康之后再加大任务复杂度。权限档可以从每步都问开始，建立信任后再放宽。团队培训时把这一段当作开学第一课。

## 22.5 低内存机器

少开评测与多个隐藏窗口。生成并发调到 1。关掉宠物动画。

在实际使用中，这类环境问题往往被误判成「模型不行」。先 doctor、看 boot.log、换一个最小任务（问现在几点、写一个 hello.md）区分是运行时坏了还是任务太难。环境健康之后再加大任务复杂度。权限档可以从每步都问开始，建立信任后再放宽。团队培训时把这一段当作开学第一课。

## 22.6 公司代理

为 Node 配 HTTPS_PROXY。部分中转要 insecure 时宁可换网关不要关全局 TLS。

在实际使用中，这类环境问题往往被误判成「模型不行」。先 doctor、看 boot.log、换一个最小任务（问现在几点、写一个 hello.md）区分是运行时坏了还是任务太难。环境健康之后再加大任务复杂度。权限档可以从每步都问开始，建立信任后再放宽。团队培训时把这一段当作开学第一课。

## 22.7 时间同步

TOTP 依赖系统时间。虚拟机时间漂移会导致 2FA 失败。

在实际使用中，这类环境问题往往被误判成「模型不行」。先 doctor、看 boot.log、换一个最小任务（问现在几点、写一个 hello.md）区分是运行时坏了还是任务太难。环境健康之后再加大任务复杂度。权限档可以从每步都问开始，建立信任后再放宽。团队培训时把这一段当作开学第一课。

## 22.8 磁盘满

对象库、视频缓存、.history 会涨。看设置缓存统计与分镜 4GB 上限。不要手动乱删正在跑的任务目录。

在实际使用中，这类环境问题往往被误判成「模型不行」。先 doctor、看 boot.log、换一个最小任务（问现在几点、写一个 hello.md）区分是运行时坏了还是任务太难。环境健康之后再加大任务复杂度。权限档可以从每步都问开始，建立信任后再放宽。团队培训时把这一段当作开学第一课。

## 22.9 杀毒软件

Windows 可能拦 Electron 解压与 run_shell。加目录白名单，不要关杀毒。

在实际使用中，这类环境问题往往被误判成「模型不行」。先 doctor、看 boot.log、换一个最小任务（问现在几点、写一个 hello.md）区分是运行时坏了还是任务太难。环境健康之后再加大任务复杂度。权限档可以从每步都问开始，建立信任后再放宽。团队培训时把这一段当作开学第一课。

## 22.10 中文路径

一般可用。若某外部引擎失败，换到 ASCII 工作目录再试。

在实际使用中，这类环境问题往往被误判成「模型不行」。先 doctor、看 boot.log、换一个最小任务（问现在几点、写一个 hello.md）区分是运行时坏了还是任务太难。环境健康之后再加大任务复杂度。权限档可以从每步都问开始，建立信任后再放宽。团队培训时把这一段当作开学第一课。


## 23. 按工作场景的详细用法

### 23.1 场景：周报

当你的真实工作是「周报」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 周报 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 周报，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 周报 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 周报（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.2 场景：月报

当你的真实工作是「月报」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 月报 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 月报，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 月报 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 月报（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.3 场景：述职

当你的真实工作是「述职」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 述职 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 述职，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 述职 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 述职（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.4 场景：立项

当你的真实工作是「立项」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 立项 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 立项，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 立项 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 立项（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.5 场景：复盘

当你的真实工作是「复盘」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 复盘 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 复盘，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 复盘 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 复盘（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.6 场景：预算

当你的真实工作是「预算」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 预算 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 预算，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 预算 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 预算（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.7 场景：招聘

当你的真实工作是「招聘」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 招聘 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 招聘，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 招聘 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 招聘（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.8 场景：入职培训

当你的真实工作是「入职培训」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 入职培训 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 入职培训，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 入职培训 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 入职培训（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.9 场景：客户回访

当你的真实工作是「客户回访」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 客户回访 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 客户回访，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 客户回访 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 客户回访（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.10 场景：投诉处理

当你的真实工作是「投诉处理」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 投诉处理 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 投诉处理，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 投诉处理 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 投诉处理（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.11 场景：产品说明

当你的真实工作是「产品说明」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 产品说明 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 产品说明，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 产品说明 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 产品说明（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.12 场景：帮助文档

当你的真实工作是「帮助文档」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 帮助文档 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 帮助文档，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 帮助文档 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 帮助文档（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.13 场景：活动策划

当你的真实工作是「活动策划」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 活动策划 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 活动策划，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 活动策划 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 活动策划（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.14 场景：海报

当你的真实工作是「海报」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 海报 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 海报，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 海报 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 海报（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.15 场景：短视频脚本

当你的真实工作是「短视频脚本」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 短视频脚本 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 短视频脚本，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 短视频脚本 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 短视频脚本（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.16 场景：播客

当你的真实工作是「播客」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 播客 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 播客，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 播客 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 播客（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.17 场景：数据清洗

当你的真实工作是「数据清洗」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 数据清洗 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 数据清洗，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 数据清洗 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 数据清洗（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.18 场景：可视化大屏

当你的真实工作是「可视化大屏」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 可视化大屏 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 可视化大屏，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 可视化大屏 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 可视化大屏（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.19 场景：信息图

当你的真实工作是「信息图」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 信息图 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 信息图，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 信息图 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 信息图（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.20 场景：公众号

当你的真实工作是「公众号」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 公众号 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 公众号，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 公众号 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 公众号（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.21 场景：小红书

当你的真实工作是「小红书」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 小红书 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 小红书，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 小红书 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 小红书（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.22 场景：邮件序列

当你的真实工作是「邮件序列」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 邮件序列 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 邮件序列，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 邮件序列 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 邮件序列（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.23 场景：合同

当你的真实工作是「合同」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 合同 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 合同，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 合同 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 合同（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.24 场景：会议

当你的真实工作是「会议」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 会议 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 会议，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 会议 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 会议（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.25 场景：行程

当你的真实工作是「行程」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 行程 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 行程，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 行程 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 行程（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.26 场景：竞品周报

当你的真实工作是「竞品周报」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 竞品周报 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 竞品周报，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 竞品周报 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 竞品周报（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.27 场景：SEO

当你的真实工作是「SEO」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 SEO 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 SEO，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 SEO 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 SEO（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.28 场景：财务测算

当你的真实工作是「财务测算」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 财务测算 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 财务测算，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 财务测算 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 财务测算（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.29 场景：角色定妆

当你的真实工作是「角色定妆」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 角色定妆 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 角色定妆，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 角色定妆 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 角色定妆（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.30 场景：分镜

当你的真实工作是「分镜」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 分镜 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 分镜，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 分镜 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 分镜（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。

### 23.31 场景：配音

当你的真实工作是「配音」时，不要把所有背景在第一句里堆成一段无法执行的散文。建议三句结构：交付物是什么文件、读者是谁、硬约束有哪几条。例如读者是「财务总监」还是「新同事」，篇幅和用词完全不同。硬约束写成可验收的句子：「必须包含去年同期对比」「不能出现未公开的营收数字」「竖版 9:16」。

权限选择：若 配音 只涉及只读材料，用 Ask 先把事实问清楚；要落盘再用 Craft。涉及对外发送、删除、覆盖模板时把档位降到「每步都问」，看 diff 再批。Goal 适合有清单的 配音，例如必须包含的章节或市州或字段。

过程中盯三件事：它有没有读你给的那份材料（过程卡应有 read_file/library_read），有没有编造材料里没有的数字，有没有把文件写到本对话目录。完成后用系统软件打开，而不是只看对话里的「已完成」。若文件打不开，这就是缺陷，用插话要求重写，不要自己在对话里将就。

材料治理：把 配音 相关的模板放进资料库或项目目录，避免每次粘贴。把「我们公司禁止的说法」放进手写记忆。把成功的流程沉淀成技能，而不是靠你每次重新教。定时需要的 配音（如周报）用 scheduler，指令必须自包含。

失败恢复：模型超时换备用渠道；缺二进制看 doctor；额度用尽停任务去充值或换本地模型；方向走偏插话；文件写错 rewind。不要在同一会话里反复用完全相同的错误命令硬撞，死循环硬停会拦你，但钱可能已经花了。
